# ENVIRONMENT.md — Machine-Specific Commands

> Fill in the placeholders once per machine. The agent uses these exact commands for
> verification and never guesses paths.

## Unity
- **Unity version (PINNED — set from the brief; API arbiter for all code):** `2021.3.45f2`
- **Key package versions locked to this editor** (fill from `Packages/manifest.json` after
  project creation):
- **Unity editor path:** `C:\Program Files\Unity\Hub\Editor\2021.3.45f2\Editor\Unity.exe`
- **Project path:** `C:\UnityProjects\PanteonCaseProject`

## Compile check (choose the mode that fits the moment)

**A. Fast solution build (preferred while iterating; catches C# errors, not asset issues):**
```
dotnet build "<ProjectName>.sln" -nologo -v q
```
Requires the solution to have been generated by Rider/Unity at least once. If the sln is
missing or stale after adding asmdefs, ask the human to regenerate (Rider does this on
project open) or fall back to B.

**B. Full Unity batchmode compile + asset import (authoritative; slower; requires the
Unity editor for this project to be CLOSED):**
```
"<UnityPath>" -batchmode -quit -projectPath "<ProjectPath>" -logFile - 
```
Exit code 0 with no `error CS` lines in the log = success. If the editor is open, ask the
human to either close it or just confirm the console is clean after Rider's edits refresh.

## Tests (Unity Test Framework, batchmode — editor must be closed)
```
"<UnityPath>" -batchmode -projectPath "<ProjectPath>" -runTests -testPlatform EditMode -testResults "<ProjectPath>/TestResults/editmode.xml" -logFile -
```
Parse the XML for failures and include a pass/fail summary in the feature report. PlayMode:
same command with `-testPlatform PlayMode` (only when a feature genuinely needs it).

## Notes for the agent
- If the human has the Unity editor open, prefer mode A and ask the human to report any
  console errors after Unity's script reload — do not silently skip verification.
- Never launch the Unity editor GUI. Batchmode only.
