# ARCHITECTURE.md — System Architecture

> Kept current as features land. This file is the map an evaluator (or the agent in a fresh
> session) uses to understand the whole project.

## 1. High-level overview
A 2D top-down strategy demo built on a discrete grid Game Board. The player places
buildings (Barracks, Power Plant) from a pooled, infinitely-scrolling Production Menu,
produces soldiers from Barracks, and commands soldiers with left-click-select /
right-click-move-or-attack. Movement uses a custom grid A* pathfinder that routes around
building obstacles. Architecture is MVC-flavored and data-driven: building/unit *stats* live
in ScriptableObject definitions, *creation* goes through Factories, *cross-system
communication* goes through C# events, and MonoBehaviours stay humble (Views/Controllers)
while plain C# classes hold the testable logic (Models) — chosen because the brief explicitly
mandates Factory, Singleton, MVC, Events, Object Pooling, and A*, and this is the
straightforward best-practice way to satisfy all of them together without contradicting each
other.

## 2. Module map

| Module | Namespace | Responsibility | Depends on |
|---|---|---|---|
| Core | `CaseGame.Core` | Bootstrapping, the one sanctioned Singleton (`GameManager`), scene/system lifecycle | Events |
| Grid | `CaseGame.Grid` | Grid data model: cell size/bounds (fully designer-editable via a `GridDefinition` SO — no fixed pixel size, set once art is chosen), occupancy, world↔cell conversion, placement validity queries. As of Report 020 also `FootprintCenterToWorld(origin, footprint)` — the true geometric center of a whole footprint rectangle, correct for both odd and even footprint sizes (decisions log #52). `GridDefinition` as of Report 029 also carries `LineColor`/`LineThickness` (data-driven grid line visuals). `GridView` (`[ExecuteAlways]`) builds the runtime `GridModel` (unchanged) and, as of Report 029, also renders the board's lines as a single combined mesh (`GridLineMeshBuilder`, pure/testable) instead of `OnDrawGizmos` — one draw call regardless of board size (GI-12), rebuilt on `Awake`/`OnValidate`/an Edit-Mode-only `Update` signature check (so tweaking `GridDefinition` previews live without Play Mode), with a public `SetLinesVisible(bool)` toggle. New `GridLines.shader`/`M_GridLines.mat` (plain vertex-color pass-through, no texture) — see decisions log #64. As of Report 031, `GridDefinition` also carries `TerrainMargin` — the single source of truth for how far the environment's water backdrop extends beyond the grid, read by both the baked terrain (Report 030) and `CameraController`'s pan/zoom bounds (decisions log #66); `GridView` exposes it via a new `Definition` property | Core |
| Entities | `CaseGame.Entities` | `GameEntityDefinition` (abstract SO: name/sprite/footprint/maxHealth) and `GameEntityBase` (abstract MonoBehaviour: owns a `Health`, implements `IDamageable`, sprite assignment, death callback, and a `protected virtual OnEntityDied()` hook called from the death path alongside the pooling callback, so a subclass can add its own death-time cleanup without event-subscription/pooled-reuse hazards, decisions log #52) — shared by Buildings and Units so neither duplicates this shape (see decisions log #22). As of Report 028, also a `public virtual GetNearestOccupiedCell(GridModel, Vector2Int)` (default: own cell) that `BuildingBase` overrides to clamp into its footprint — lets `SoldierBase`'s attack-range checks treat buildings/units identically without `Units` referencing `Buildings` (decisions log #63). `SetSelected(bool)` (Report 015) toggles a dedicated `outlineRenderer` child (`SpriteSelectionOutline` shader, decisions log #56) rather than tinting the real sprite, since Report 022. As of Report 034, also a `protected SetFlippedHorizontally(bool)` mirroring both `spriteRenderer`/`outlineRenderer` via `SpriteRenderer.flipX` (human-requested unit facing) — reset to unflipped in `Initialize` for the same pooled-reuse-hazard reason `outlineRenderer.enabled` already gets reset there (decisions log #38/#56) — see decisions log #69 | Combat |
| Buildings | `CaseGame.Buildings` | `BuildingBase` (: `GameEntityBase` — virtual `SpawnPosition` (Report 017, decisions log #42); owns its own placement state (`FootprintOrigin`, `SetPlacement(GridModel, origin)`) and a public `ReleaseFootprint()` — called from `OnEntityDied()` for combat death, and directly by `PlacementController` for manual removal, two independent triggers that share the same cleanup (decisions log #52/#55); as of Report 028 also overrides `GetNearestOccupiedCell` to clamp an attacker's cell into its footprint rectangle, decisions log #63), `Barracks` (overrides `SpawnPosition`), `PowerPlant`, `BuildingDefinition` (: `GameEntityDefinition`; `ProducibleUnits` is `List<UnitCatalogEntry>`, decisions log #41), `BuildingFactory` (`Release(instance)` looks up the instance's own pool internally — no caller needs to pass back the originating prefab, decisions log #55); also `BuildingCatalogEntry`/`BuildingCatalog` (Report 014), `BuildingCatalogEntryEventChannel` (decisions log #32; as of Report 035 also subscribed to by `Selection` — see decisions log #70), `SelectedBuildingEventChannel` (Report 015), `BuildingRemovalRequestedEventChannel` (decisions log #55 — carries a manual-removal request, deliberately independent of Health) | Entities, Grid, Pooling, Units, Events |
| Units | `CaseGame.Units` | `SoldierBase` (: `GameEntityBase`, adds `MoveTo`/`Attack`; `MoveTo`'s `FollowPath` coroutine walks each path step over a fixed `StepDuration(moveSpeed)` — orthogonal and diagonal steps take the same time, both counting as exactly 1 cell, via the pure/testable `StepDuration`/`InterpolateStep` static helpers, decisions log #57). As of Report 027, `Attack` replaces the old instant `TryAttack`: a single `_actionCoroutine` slot shared with `MoveTo` (a move order cancels an attack and vice versa; a new `Attack` call on a different target switches onto it immediately — no "attack lock"), walking into `UnitDefinition.AttackRange` first via `AStarPathfinder.FindApproachCell` if needed, then a sustained tick loop (`AttackInterval`) until the target dies or leaves range (pure/testable `ChebyshevDistance`/`IsInRange`/`AttackInterval` helpers) — see decisions log #62. As of Report 028, range/approach checks use `target.GetNearestOccupiedCell(...)` rather than the target's raw `transform.position` cell, so multi-cell buildings are attacked/approached correctly from whichever footprint edge is nearest (decisions log #63). As of Report 033, `SoldierBase` also holds an optional `[SerializeField] Animator animator` (mirrors `GameEntityBase`'s direct `spriteRenderer`/`outlineRenderer` references) driving two parameters built by a throwaway script directly on the Tiny Swords Warrior/Archer/Pawn Animator Controllers (which ship with every clip present but zero parameters/transitions wired) — a bool `IsMoving` (set for `FollowPath`'s duration, reset in `CancelAction` too so an interrupted move never leaves it stuck on) and a trigger `Attack` (fired once per `PerformAttack` call) — see decisions log #68. As of Report 034, `FollowPath`/`PerformAttack` also call the new `GameEntityBase.SetFlippedHorizontally` based on movement/attack-target X direction (pure/testable `FacesLeft` helper), and ranged `PerformAttack` defers its `Projectile` launch to a new public `ReleaseAttack()` — called via a `SoldierAnimationEvents` relay component (new, lives on the `Visuals` child alongside the `Animator`) wired to an Animation Event on the Shoot clip, so the arrow visibly leaves the bow at that point in the animation rather than the instant the attack tick started (human-requested; melee's instant `ApplyDamage` is unchanged) — see decisions log #69. `Soldier` (the one concrete type, decisions log #26), `UnitDefinition` (: `GameEntityDefinition`; `MoveSpeed` is cells/second; as of Report 027 also `Ranged`/`AttackRange`/`AttackSpeed`), `UnitFactory` (as of Report 020 also tracks `ActiveUnits` — a live set of everything it's created and hasn't released, used for spawn-cell occupancy checks, decisions log #54); also `UnitCatalogEntry`, `UnitProductionRequest`/`UnitProductionRequestEventChannel`, `UnitProductionController` (Report 020: now also snaps the spawn position to a grid cell and blocks production if that cell is occupied — by a building or another unit, see decisions log #54), and as of Report 027 `Projectile`/`ProjectileFactory` (a pooled, purely-visual ranged-attack indicator — no `Collider`, tracks its target's live position every frame, applies damage on arrival; own dedicated `PrefabPool<Projectile>`, decisions log #62; as of Report 028 also rotates to face its travel direction each frame via the pure `FacingRotation` helper and an inspector-tunable `spriteForwardOffsetDegrees`, decisions log #63) | Entities, Grid, Pathfinding, Combat, Pooling, Events |
| Combat | `CaseGame.Combat` | `IDamageable`, `Health` (HP, damage, death via plain per-instance C# events — see decisions log #19) | — |
| Pathfinding | `CaseGame.Pathfinding` | `AStarPathfinder`: static, tested 8-directional grid A* with corner-cutting prevention ("wander around buildings," GI-7/8). No request-queue/coroutine layer yet — added if a real caller (Units movement) shows it's needed. As of Report 027 also `FindApproachCell(grid, from, target, range)` — nearest unoccupied cell within range of a target, ranked by straight-line distance to the attacker; used for "walk into attack range, then attack" (decisions log #62) | Grid |
| Placement | `CaseGame.Placement` | `BuildingGhostView` (View: toggles a desaturated ghost silhouette, tinted green/red, vs. the real sprite — see decisions log #28) and `PlacementController` (Controller: mouse→cell, validity query, commit-to-grid, and — as of Report 014 — subscribes to `BuildingCatalogEntryEventChannel` to start placement when the Production Menu raises a "produce" request; as of Report 017, `Update()` also skips world input while the pointer is over UI, see decisions log #47). The cell passed to `UpdateGhostAt`/`TryCommitAt` is the footprint's *center*, not its bottom-left corner — converted internally via `GridModel.FootprintCenterToWorld` — and `TryCommitAt` calls the committed instance's `BuildingBase.SetPlacement` (decisions log #52). Also subscribes to `BuildingRemovalRequestedEventChannel`: `RemoveBuilding` frees the footprint and returns the instance to its pool directly, entirely independent of Health — Placement owns both "commit to" and "uncommit from" the grid (decisions log #55) | Grid, Buildings, Events |
| Selection | `CaseGame.Selection` | `SelectionController`: left-click select (plain click replaces, shift-click adds/removes a soldier — GI-7/8's "unit(s)"), right-click move-or-attack (GI-7/8/10/11 — attack takes priority over movement when the cursor is over a `GameEntityBase`). Selecting a building and selecting soldiers are mutually exclusive; visual feedback is `GameEntityBase.SetSelected`, no new prefab wiring. Raises `SelectedBuildingEventChannel` on building-selection change; as of Report 017, `Update()` also skips world input while the pointer is over UI (decisions log #47). `SetSelectedBuilding` defensively clears a stale reference to a building that died in *combat* before comparing — mirrors the existing soldier-pruning pattern (decisions log #39). Also subscribes to `BuildingRemovalRequestedEventChannel` (`HandleBuildingRemoved`) to proactively clear the selection when the selected building is manually removed — removal doesn't set `IsDead` the way combat death does, so the lazy staleness check alone can't catch it (decisions log #55). As of Report 027, `HandleRightClick`'s target parameter is `GameEntityBase` (was `IDamageable`) so it can be passed straight through to `SoldierBase.Attack`, and `Initialize` also takes a `ProjectileFactory` to forward along (decisions log #62). Report 034 gave `SelectionController` a direct `PlacementController` reference and an `IsPlacing`-checking guard in `Update()`, intended to stop a placement cancel/commit click from also acting on the selection underneath the ghost — as of Report 035, this was replaced (the guard was racy: both controllers read the same click in their own `Update()`, with no guaranteed order, so the guard only worked when Placement happened to run second): `SelectionController` now instead subscribes to the same `BuildingCatalogEntryEventChannel` `PlacementController` already reacts to, and clears the current selection the instant a placement is requested — no reference between the two controllers at all, see decisions log #70 | Units, Buildings, Grid, Events |
| Gameplay | `CaseGame.Gameplay` | `GameplayBootstrap` (Report 017) — `Gameplay.unity`'s composition root: the one place that builds `BuildingFactory`/`UnitFactory`/(Report 027) `ProjectileFactory` with a real container `Transform` and calls `PlacementController`/`SelectionController`/`UnitProductionController`'s `Initialize` from `Start()` (runs after every object's `Awake`, unlike relying on Awake-order between them and whoever owns the `GridModel`). Holds no gameplay logic itself — see decisions log #45 for why this is its own module rather than living in Core. As of Report 031, `Start()` also computes the environment's water-backdrop bounds (`GridModel.CellToWorld` ± `GridDefinition.TerrainMargin`) and calls `CameraController.SetBounds` | Grid, Buildings, Units, Placement, Selection, CameraControl |
| CameraControl | `CaseGame.CameraControl` | `CameraController` (Report 019, human-requested quality-of-life, not a brief requirement) — middle-mouse-drag pan, scroll-wheel zoom (clamped), both skipped while the pointer is over UI (same `IsPointerOverGameObject` guard as Placement/Selection, decisions log #47). Named `CameraControl`, not `Camera`, to avoid a namespace/type collision with `UnityEngine.Camera` (decisions log #51). As of Report 031, gained `SetBounds(min, max)` — now the only controller `GameplayBootstrap` initializes for a reason *other* than needing a `GridModel`/Factory reference, since pan/zoom now clamp to the environment's water-backdrop rectangle via a pure/testable `ClampToBounds` (human-requested: the camera must never show the background color/skybox past the water's own edge) — see decisions log #66. As of Report 034, raw `Mouse.scroll` deltas are converted to "notches" via a new pure `NotchesFromRawScrollDelta` (÷120, matching Windows' `WHEEL_DELTA`) before reaching `Zoom` — the Input System reports ±120 per physical notch, not a small ±1-ish value, so `zoomSpeed` was previously being multiplied by ~120× too much, letting a single notch blow straight through the whole min/max range (human-reported) — see decisions log #69 | Grid |
| Environment | `CaseGame.Environment` | `BorderDecorationLayout` (Report 030, pure/testable static class) — computes an evenly-spaced ring of decoration placements around a `GridModel`'s expanded bounds, cycling through N sprite variants. No MonoBehaviour: this pass's terrain (a water `Tilemap` frame + a ring of rock `SpriteRenderer`s) is static, one-time-authored scene content baked by a throwaway editor script, not a runtime system — see decisions log #65. As of Report 031, the water frame's extent is painted from `GridDefinition.TerrainMargin` (30 world units, extended from Report 030's hardcoded 20) instead of a script-local constant, so it always matches `CameraController`'s clamp bounds (decisions log #66). As of Report 032, the human's hand-painted grass/cliff island (Report 031) is now procedurally regenerated for any grid size: `IslandTileSet` (SO, 12 named `TileBase` references — 9 for the grass 9-slice, 3 for the cliff row), `IslandTilemapLayout` (pure/testable static class computing grass/cliff tile placements for any `columns`/`rows`), and `IslandTerrainView` (`[ExecuteAlways]` view, mirrors `GridView`'s Report 029 live-preview pattern exactly — rebuilds on `Awake`/`OnValidate`/an Edit-Mode `Update` signature check) — see decisions log #67 | Grid |
| Pooling | `CaseGame.Pooling` | Generic `ObjectPool<T>` used by the scroll view and by frequently spawned/destroyed units/buildings | — |
| Events | `CaseGame.Events` | Lightweight C# event channels (plain events and/or SO event channels) connecting the above without direct references | — |
| UI.Production | `CaseGame.UI.Production` | `ScrollRecycler` (plain C#: given item count/pool size/scroll offset, decides which data index each pooled slot shows and where — the actual "infinite, object-pooled scroll" math, tested independent of Unity UI), `ProductionMenuItemView` (View: one pooled, rebindable row), `ProductionMenuController` (Controller: owns the `PrefabPool<ProductionMenuItemView>`, applies `ScrollRecycler`'s decisions to `ScrollRect`). Iterates `BuildingCatalog` **generically** (no per-type UI branches); a row's "produce" click raises `BuildingCatalogEntryEventChannel` — Placement listens, so neither module references the other. As of Report 026, `ProductionMenu.prefab`'s root is trimmed to the bottom 94% of its own height, freeing a strip at the top for a `ProductionMenuBanner` (plain TMP text + background `Image`, added as a Canvas-level sibling in `Gameplay.unity`, not part of the prefab) — decisions log #61 | Pooling, Buildings, Events |
| UI.Info | `CaseGame.UI.Info` | `InfoPanelController` (subscribes to `SelectedBuildingEventChannel`; shows the selected building's image/name, hides entirely for null/soldier selection) and `ProducibleUnitIconView` (icon per producible unit, spawned generically off `BuildingDefinition.ProducibleUnits` — requirement 6's "Power Plant needs no production sub-menu" falls out for free as an empty list, no per-type branch). As of Report 017 these icons are **clickable** — this row *is* GI-6's "production sub-menu," so clicking one raises `UnitProductionRequestEventChannel` (`CaseGame.Units`); see decisions log #44 for why Report 016 originally built this non-interactive and what changed. The panel also has a "Remove Building" button (`RequestRemoveBuilding`) — human-requested, not brief-mandated — that raises `BuildingRemovalRequestEventChannel` with the selected building; deliberately does *not* call `ApplyDamage` — manual removal and combat death are kept as separate triggers (decisions log #55, superseding #53's original ApplyDamage-reuse design). As of Report 024, `ProducibleUnitsContainer` (in `InformationPanel.prefab`) uses a `GridLayoutGroup` (Fixed Column Count, shape/size fully adjustable via its own Inspector fields) plus a `ContentSizeFitter` (auto-height) instead of a non-wrapping `HorizontalLayoutGroup` — no `InfoPanelController`/`ProducibleUnitIconView` code change needed, since the wrapping is entirely a built-in-component concern (decisions log #58). A same-day follow-up added a `VerticalLayoutGroup` to `PanelContent` (its parent) so `BuildingIcon`/`BuildingName`/`ProducibleUnitsContainer`/`RemoveButton` stack top-to-bottom instead of each being independently anchored — `RemoveButton` now correctly gets pushed down/up as the grid's own content-driven height changes with the producible-unit count, instead of overlapping it at a fixed position (decisions log #59). `SetSelectedBuilding` also now forces an immediate layout rebuild after spawning icons, and icon cleanup uses `DestroyImmediate` unconditionally (was `Destroy` in Play Mode) — without both, the panel briefly showed the *previous* selection's stale layout, self-correcting only on the next selection change (decisions log #60). As of Report 026, `PanelContent` also has a static "Information" banner as the first element of its `VerticalLayoutGroup` stack (Report 025) — shown/hidden alongside the rest of the panel's content rather than always-on, a deliberate scope choice to avoid touching `panelRoot`'s existing show/hide semantics (decisions log #61) | Events, Buildings, Units |
| UI.MainMenu | `CaseGame.UI.MainMenu` | Main Menu screen: Play button → loads `Gameplay.unity`. As of Report 026, `MainMenuController` also routes a "How to Play" button/panel (`howToPlayPanel`/`howToPlayButton`/`howToPlayBackButton`), mirroring the existing Settings button/panel/back-button pattern exactly — a static controls-explanation screen, no new state beyond what `ShowMainPanel`/`OnBackClicked` already generalize to a third panel (decisions log #61) | Core |
| UI.Settings | `CaseGame.UI.Settings` | Settings screen: resolution / display-mode selection, applied via `Screen.SetResolution` | Core |

MVC mapping: **Model** = `BuildingDefinition`/`UnitDefinition` (SO data) + `Health`/combat
state (plain C#); **View** = MonoBehaviours rendering sprites/UI only; **Controller** =
MonoBehaviours in Selection/Placement/UI.* that translate input and model changes into view
updates, publishing/subscribing via Events rather than reaching into each other directly.

## 3. Data flow & communication
- **Definitions (Model data):** `BuildingDefinition` and `UnitDefinition` ScriptableObjects
  hold name, sprite, footprint dimensions, max HP, and (for buildings) damage-per-attack or
  produced-unit list. Designers add new building/unit types by adding a new SO asset +
  minimal subclass, not by editing unrelated systems (Open Question #1 in BRIEF.md).
- **Creation:** `BuildingFactory`/`UnitFactory` (Factory pattern) take a `Definition` and
  return a configured, pooled instance. No `new` scattered across controllers.
- **Selection → Info Panel:** `Selection` raises a `SelectionChanged` event carrying the
  selected object's definition/state; `UI.Info` subscribes and renders — no direct reference
  from selection logic to UI.
- **Move/Attack commands:** right-click while soldiers are selected raises a `MoveCommand` or
  `AttackCommand` event (context-dependent on what's under the cursor); the soldier's
  controller consumes it, requests a path from `Pathfinding`, and moves along it via
  coroutine, or resolves an attack via `Combat`.
- **HP/Death:** `Health.Damaged`/`Health.Died` events drive destruction (return to pool)
  without the attacker needing to know about pooling or scene cleanup.
- **Placement validity:** `Placement` queries `Grid` for occupancy/bounds each frame while a
  ghost is active, and toggles the red/valid visual purely from that query — no coupling to
  specific building types.
- No save/load path (out of scope per BRIEF.md).

## 4. Scene & prefab composition
- `Boot.unity` — persistent systems only: `GameManager` (Singleton), event channels, pools.
  Loads `MainMenu.unity` on start.
- `MainMenu.unity` — Play button → loads `Gameplay.unity`; Settings screen for
  resolution/display-mode (demonstrates GI-13, human-mandated requirement 19).
- `Gameplay.unity` — the actual demo. As of Report 017, fully assembled:
  - `--- ENVIRONMENT ---`: `Main Camera` (orthographic, reframed on the grid — see decisions
    log #48), `Global Light 2D`, `Grid` (`GridView`, wired to `GridDef_Default.asset`).
  - `--- SYSTEMS ---`: `PlacementController` (wired: `gameCamera` = Main Camera,
    `produceRequestedChannel` = `BuildingCatalogEntryEvent_Default.asset`), `SelectionController`
    (wired: `gameCamera` = Main Camera, `selectedBuildingChannel` = `SelectedBuildingEvent_Default.asset`),
    `UnitProductionController` (wired: `produceRequestedChannel` = `UnitProductionRequestEvent_Default.asset`),
    `GameplayBootstrap` (wired to `Grid`'s `GridView` and the three controllers above, plus the
    `--- GAMEPLAY ---` containers below — `Start()` builds the Factories and calls each
    controller's `Initialize`).
  - `--- GAMEPLAY ---`: `Buildings` and `Units` (empty container Transforms — `BuildingFactory`/
    `UnitFactory`'s pooled instances parent here at runtime).
  - `--- UI ---`: `Canvas` (`ScaleWithScreenSize`, 1920×1080 reference), `EventSystem`
    (`InputSystemUIInputModule`), `ProductionMenu` (`ProductionMenuController`, left-anchored,
    wired to `BuildingCatalog_Default.asset`), `InformationPanel` (`InfoPanelController`,
    right-anchored, starts inactive, wired to `SelectedBuildingEvent_Default.asset`). Human hand-
    adjustment after hand-testing Report 017: `ProductionMenu`'s `ScrollRect.movementType` is now
    `Clamped` (was the Unity default `Elastic`) — worked around a bug where dragging the scroll
    fast enough loses the top-of-list items (not the bottom ones); see the roadmap in
    `CURRENT_STATUS.md` for the planned real fix.
- Key prefabs, all under `Assets/_Project/Prefabs/`:
  `Buildings/Building_Barracks.prefab` + `Building_PowerPlant.prefab` (human-created after
  Report 009, `Hitbox` collider added per Report 015's hookup) — a parent GameObject with
  `Visuals` (SpriteRenderer), `VisualsGrayscale` (Report 013's ghost), `Hitbox` (`BoxCollider2D`,
  trigger), `Outline` (Report 022's selection outline, decisions log #56), and (Barracks only)
  `SpawnPoint` as children. `Units/Soldier_1.prefab`,
  `Soldier_2.prefab`, `Soldier_3.prefab` (human-created after Report 012, same `<Category>_<Name>`
  naming, `Hitbox` collider added per Report 015) — same `Visuals`/`Hitbox`/`Outline` child
  structure, `Soldier` component on the root. `UI/ProductionMenuItem.prefab` (Report 014) — `Icon`
  (Image) + `Name` (TMP text) children, `Button` + `ProductionMenuItemView` on the root; no
  `<Category>_` prefix since there's no family of named variants to disambiguate.
  `UI/ProducibleUnitIcon.prefab` (Report 016, **rebuilt in Report 017** with an added `Button` —
  see decisions log #44) — same `Image`/`Name` shape, now clickable. All agent-created prefabs
  built/rebuilt via throwaway editor scripts, verified by reading the generated files back
  rather than trusting a clean exit code. All per CONVENTIONS.md's per-prefab grouping
  convention. Ghost/preview prefab for placement: none needed, reuses the building prefabs
  themselves (decisions log #29).
- SO definition/config assets live under `Assets/_Project/ScriptableObjects/`: `GridDef_Default.asset`,
  `BuildingCatalog_Default.asset`, `BuildingCatalogEntryEvent_Default.asset` (Report 014),
  `SelectedBuildingEvent_Default.asset` (Report 016), and `UnitProductionRequestEvent_Default.asset`
  (Report 017) at the root, `GameEntityDefs/Buildings/BuildingDef_Barracks.asset` +
  `BuildingDef_PowerPlant.asset` (Report 017: `producibleUnits` populated with Soldier 1/2/3;
  `entityName` fixed — see decisions log #46), and `GameEntityDefs/Units/UnitDef_Soldier1.asset` +
  `UnitDef_Soldier2.asset` + `UnitDef_Soldier3.asset` (Report 017: `entityName`/Soldier 3's stats
  fixed, same decision) — human's own organization, renamed from an earlier
  `ScriptableObjects/Units/{Buildings,Troops}/` pass to `GameEntityDefs/` once the
  `GameEntityDefinition` shared base landed (Report 010), which is a more accurate name for
  what the folder actually holds. `GameEntityDefs/` is reserved for `GameEntityDefinition`
  subclasses specifically — `BuildingCatalog`/event channels are config/data assets, not
  entity definitions, so they stay at the SO root alongside `GridDef_Default.asset`.
- Third-party art: `Assets/Tiny Swords/` (a purchased/free asset pack, human-imported during
  visual polish, not agent-authored) — building/unit sprites now come from here (`Buildings/Blue
  Buildings/`, `Units/Blue Units/`) instead of the earlier placeholder sprites. Lives at the
  `Assets/` root rather than under `_Project/`, which is normal/expected for an imported
  third-party package (CONVENTIONS.md's "nothing project-authored goes outside `_Project`" rule
  is about *project-authored* content — this is imported, unmodified third-party content, the
  same category `Plugins/` exists for, just not literally renamed into that folder since Unity's
  Package Manager/Asset Store import flow creates its own top-level folder).
- New this report (018): `Assets/_Project/Art/Textures/SpriteAtlas_Gameplay.spriteatlas` — see
  decisions log #49.
- This section is updated feature-by-feature as prefabs/assets are actually created — treat
  the above as current inventory as of Report 018.

## 5. Brief-mandated requirements checklist
Tracks BRIEF.md's "Hard requirements" 1–19 by number. No single numbered requirement is
fully done yet — see the implementation log below the table for foundation work already
landed that these requirements will build on.

**Implementation log:**
- Report 001 (`Docs/Reports/001_grid-foundation.md`): `GridDefinition` (SO), `GridModel`
  (plain C#, tested), `GridView` (Gizmo visualization) landed under
  `Assets/_Project/Scripts/Runtime/Grid/`. Foundation only — not yet wired into a scene, not
  yet consumed by Placement/Buildings/Pathfinding. No requirement below is checked off by
  this alone.
- Report 002 (`Docs/Reports/002_core-singleton.md`): `GameManager` (the brief-mandated
  Singleton, isolated behind `IGameManager`) landed under `Assets/_Project/Scripts/Runtime/Core/`.
  Handles cross-scene persistence and scene-transition entry point. Not yet wired into
  `Boot.unity` (scene doesn't exist yet). Automated tests dropped for this feature — see
  `ENVIRONMENT.md` note on EditMode `Awake` not firing reliably in batchmode; hand-tested
  instead.
- Report 003 (`Docs/Reports/003_gamemanager-root-persistence-fix.md`): fixed a bug the human
  found during Report 002's hand-test — `DontDestroyOnLoad` only accepts root GameObjects, but
  `GameManager` lives under the `--- SYSTEMS ---` organizer per CONVENTIONS.md, so it was
  throwing. Now targets `transform.root.gameObject`.
- Report 004 (`Docs/Reports/004_scene-reference-asset-picker.md`): added `SceneReference`
  (`CaseGame.Core`) — a reusable, Inspector-friendly scene-asset-picker wrapper — plus its
  `SceneReferenceDrawer` (first script under `Assets/_Project/Scripts/Editor/`, new
  `CaseGame.Editor` asmdef). `GameManager.firstScene` now uses it instead of a raw string
  field. Project convention going forward: prefer typed asset-picker wrappers over raw string
  fields for asset references (see `CONVENTIONS.md` overrides).
- Report 005 (`Docs/Reports/005_main-menu-and-settings.md`): implements requirement 19 — Main
  Menu (`MainMenuController`) with Play/Settings navigation and a Settings screen
  (`SettingsController` + tested `ResolutionOptionsBuilder`) for resolution/fullscreen, built on
  TextMeshPro (`TMP_Dropdown` for resolution selection) and the Input System package's
  `InputSystemUIInputModule` — both installed by the human specifically for this feature, with
  Input System also intended for gameplay input later. Also relocated `Boot.unity`/
  `Gameplay.unity` from `Assets/Scenes/` to `Assets/_Project/Scenes/` (human-approved hygiene
  fix) and added `MainMenu.unity` there; scene flow `Boot → MainMenu → Gameplay` is fully wired
  end-to-end (Build Settings, `GameManager`, `MainMenuController`), pending only the human's
  hand-test confirmation. (Redone in place per human instruction — the original legacy-UI/no
  packages version of this feature was superseded, not shipped as a separate feature.)
- Report 006 (`Docs/Reports/006_events.md`): landed the `Events` module foundation —
  `GameEvent` (parameterless, `[CreateAssetMenu]` SO signal channel), generic
  `GameEventChannel<T>` base for future typed payload channels, and `GameEventListener`
  (bridges a `GameEvent` to a designer-configured `UnityEvent`) — under
  `Assets/_Project/Scripts/Runtime/Events/`. Foundation only, like Grid/Core before it: no
  concrete payload channels exist yet since no gameplay types (BuildingDefinition, Health,
  etc.) exist to carry — those land alongside whichever future feature first needs one.
- Report 007 (`Docs/Reports/007_pooling.md`): landed the `Pooling` module foundation —
  `PrefabPool<T>` (generic, `T : Component`), a thin wrapper around Unity's built-in
  `UnityEngine.Pool.ObjectPool<T>` (confirmed available on `2021.3.45f2` by compile) handling
  instantiate/activate/deactivate/destroy so callers just `Get()`/`Release()`. Foundation only:
  no concrete pooled type (scroll-view list items, soldiers, buildings) exists yet — this
  lands alongside whichever future feature first needs one (Production Menu is next in line).
- Report 008 (`Docs/Reports/008_combat.md`): landed the `Combat` module foundation —
  `IDamageable` (contract) and `Health` (plain C# HP state, damage/clamp/death logic, tested)
  under `Assets/_Project/Scripts/Runtime/Combat/`. Foundation only: no soldier/building type
  exists yet to own a `Health` instance with the brief's actual numbers (requirements 9/10),
  and nothing yet calls `ApplyDamage` from an attack command (requirement 11) — those land
  with Units/Buildings/Selection. Refines the Phase-0 assumption that Combat depends on
  Events: `Damaged`/`Died` are plain per-instance C# events, not a shared `GameEventChannel<T>`
  (see decisions log #19).
- Report 009 (`Docs/Reports/009_buildings.md`): landed the `Buildings` module —
  `BuildingDefinition` (SO), `BuildingBase` (abstract, humble, implements `IDamageable` via a
  `Health` it owns), `Barracks` (adds a spawn point, GI-7), `PowerPlant` (no additions — only
  building type with no producible units, GI-6), and `BuildingFactory` (Factory pattern,
  pools instances via the already-built `PrefabPool<T>`) — all under
  `Assets/_Project/Scripts/Runtime/Buildings/`. Also added a minimal `UnitDefinition` (SO,
  data only) under `Assets/_Project/Scripts/Runtime/Units/` — a prerequisite for
  `BuildingDefinition`'s producible-units list (requirement 2); the full Units feature
  (`SoldierBase`, `Soldier1/2/3` behavior, `UnitFactory`) is still a separate future feature.
  Foundation-ish but one step closer to shippable: no scene has an actual placed building yet
  (that's Placement's job), and no requirement below is checked off — the human still needs
  to create the two concrete `BuildingDef_Barracks`/`BuildingDef_PowerPlant` assets and
  prefabs per the report's editor hookup checklist.
- Report 010 (`Docs/Reports/010_shared-entity-base.md`): extracted `GameEntityDefinition` and
  `GameEntityBase` (new `Entities` module) out of `BuildingDefinition`/`BuildingBase`, per the
  human's suggestion. `BuildingDefinition`/`UnitDefinition` now both extend
  `GameEntityDefinition`; `BuildingBase` now extends `GameEntityBase` (and just re-exposes
  `Definition` as the strongly-typed `BuildingDefinition`). Removes the field/logic
  duplication that existed between Buildings' and Units' definitions and bases, and gives the
  brief's mandated "Inheritance" requirement a real hierarchy to stand on ahead of the future
  `SoldierBase : GameEntityBase`. Human also hand-adjusted the project's actual folder/prefab
  structure after Report 009 (`ScriptableObjects/Units/{Buildings,Troops}/` instead of
  `Settings/`; prefabs use child GameObjects for `Visuals`/`Hitbox`/`SpawnPoint`) — recorded in
  `CONVENTIONS.md`.
- Report 011 (`Docs/Reports/011_pathfinding.md`): landed the `Pathfinding` module —
  `AStarPathfinder` (static, plain C#, tested): 8-directional grid A* over `GridModel`, with
  corner-cutting prevented (a diagonal step is rejected if either flanking orthogonal cell is
  blocked) so a path can't clip through a building's corner — this is the actual mechanism
  behind requirement 8's "wander around the buildings." No coroutine-based path-request queue
  yet — nothing consumes pathfinding until Units/Selection exist, so a queued/async layer
  would be speculative before a real caller reveals whether one's even needed.
- Report 012 (`Docs/Reports/012_units.md`): landed the `Units` module — `SoldierBase`
  (`: GameEntityBase`, adds `MoveTo` — pathfinds via `AStarPathfinder` then walks the route in
  a Coroutine, the brief-mandated Coroutine pattern's natural home per §3's data-flow — and
  `TryAttack`), `Soldier` (the one concrete class), `UnitFactory` (Factory pattern, pools via
  `PrefabPool<T>`, mirrors `BuildingFactory`), and extended the existing `UnitDefinition` with
  `MoveSpeed`. The brief's 3 soldier types ship as 3 `UnitDefinition` assets sharing the one
  `Soldier` class, not 3 separate classes — see decisions log #26. Still no scene has an actual
  soldier in it (that's Selection/Production's job to spawn and command one) — the human needs
  to create the 3 `UnitDef_Soldier{1,2,3}` assets and prefabs per the editor hookup checklist.
- Report 013 (`Docs/Reports/013_placement.md`): landed the `Placement` module — implements
  requirement 3's ghost/invalid-area feedback. `BuildingGhostView` toggles a building instance
  between a desaturated "ghost" silhouette (new `Art/Shaders/SpriteGrayscaleGhost.shader`,
  tinted green/red via `SpriteRenderer.color`) and its real sprite, on the *same* pooled
  instance — no separate temp-preview object. `PlacementController` drives it: mouse → grid
  cell, validity query each frame (`GridModel.IsAreaFree`, no per-building-type coupling),
  commit marks the grid occupied and reveals the real sprite, cancel returns the instance to
  its pool (new `BuildingFactory.Release`). Human-directed design: the grayscale-then-tint
  technique (not tinting the full-color sprite directly) — see decisions log #28.

- Report 014 (`Docs/Reports/014_ui-production.md`): landed the `UI.Production` module —
  the Production Menu's infinite, pooled scroll view (UX brief: "Infinite Scrollview — Object
  Pooling"). `ScrollRecycler` (plain C#, tested) computes which `BuildingCatalog` index each of
  a small, fixed-size pool of `ProductionMenuItemView` rows should currently show as the view
  scrolls — the pool never grows with the catalog size, which is what makes the list genuinely
  "infinite"-capable rather than one row per entry. A row's "produce" click raises the new
  `BuildingCatalogEntryEventChannel` (the project's first concrete `GameEventChannel<T>`
  payload channel, foreshadowed as a foundation-only placeholder back in Report 006);
  `PlacementController` now subscribes to it and calls its existing `BeginPlacement`, so
  UI.Production and Placement stay fully decoupled — connected only via the shared channel
  asset. Also added `BuildingCatalogEntry`/`BuildingCatalog` (`CaseGame.Buildings`) as the
  Production Menu's data source — a designer adds a producible building by adding one entry to
  this asset, no code change (requirement 2's modularity mandate). A throwaway editor script
  (`Scripts/Editor/Setup/Temp/ProductionMenuSetup.cs`, deleted same turn per CLAUDE.md's editor
  script policy) built `Gameplay.unity`'s first UI: a `--- UI ---` organizer with a Canvas
  (`ScaleWithScreenSize`, 1920×1080 reference — same settings as `MainMenu.unity`'s, for
  GI-13 consistency) and `EventSystem`/`InputSystemUIInputModule`, the pooled scroll view
  itself, and the `ProductionMenuItem` prefab (`Prefabs/UI/`) — populated the real
  `BuildingCatalog_Default.asset` with the existing Barracks/Power Plant definitions and
  prefabs. Scope boundary: `PlacementController` is *not yet* added to `Gameplay.unity` (it has
  nothing to be initialized with — no `GridModel`/`BuildingFactory` bootstrap exists in the
  scene yet), so the Production Menu's "produce" click can't be hand-tested end-to-end until
  Gameplay scene assembly (a later roadmap item) wires that up; this feature's own logic is
  fully unit-tested independent of that.

- Report 015 (`Docs/Reports/015_selection.md`): landed the `Selection` module —
  `SelectionController` (`CaseGame.Selection`): left-click selects (plain click replaces the
  current selection, shift-click adds/removes a soldier — a deliberately minimal multi-select
  mechanism, see decisions log #36), right-click on selected soldiers either attacks
  (`SoldierBase.TryAttack`, instant, no range/approach) or moves (`SoldierBase.MoveTo`) depending
  on whether the cursor is over an `IDamageable`. Building selection and soldier selection are
  mutually exclusive "modes." Hit-testing uses `Physics2D.OverlapPoint` against `Hitbox`
  colliders (new — the building/unit prefabs' `Hitbox` children have been empty placeholders
  since Reports 009/012, "for future selection"; this is that future). Visual selection feedback
  reuses `GameEntityBase.SetSelected` (new, tints the existing `Visuals` sprite) rather than a
  new prefab child — also fixed a latent pooling bug this surfaced: `Initialize` now resets
  `spriteRenderer.color` to white, since without it a pooled instance released while selected
  would start its next life pre-tinted. Added `SelectedBuildingEventChannel`
  (`CaseGame.Buildings`) — Selection raises it on building-selection change; nothing subscribes
  yet (UI.Info, not yet built, is the intended consumer — requirement 5). Scope boundary:
  `SelectionController` is *not* added to `Gameplay.unity` yet, same reasoning as
  `PlacementController` in Report 014 — no scene bootstrap exists to `Initialize` it with a
  `GridModel`. Editor hookup: the 5 existing building/soldier prefabs need a `BoxCollider2D`
  (trigger) added to their `Hitbox` child — full checklist in the report.

- Report 016 (`Docs/Reports/016_ui-info.md`): landed the `UI.Info` module — the Information
  Panel (requirement 5). `InfoPanelController` subscribes to `SelectedBuildingEventChannel`
  (raised by `SelectionController`, Report 015 — first real consumer) and shows the selected
  building's image/name, hiding entirely when nothing (or a soldier) is selected.
  `ProducibleUnitIconView` is a small, non-interactive, ungrouped icon (unlike
  `ProductionMenuItemView` — producing still only happens via the Production Menu) spawned
  generically off `BuildingDefinition.ProducibleUnits`; Power Plant's empty list means its row
  is simply empty — no per-building-type branch anywhere (requirement 6). Deliberately *not*
  pooled (decisions log #40) — the list is always small and only rebuilds on the rare
  "selection changed" event, unlike the Production Menu's actually-infinite list. A throwaway
  editor script (deleted same turn) built `Gameplay.unity`'s Information Panel — a right-anchored
  panel under the existing `--- UI ---` Canvas, mirroring the Production Menu's left-anchored
  strip — the `ProducibleUnitIcon` prefab, and populated `SelectedBuildingEvent_Default.asset`.
  Scope boundary, same as Reports 014/015: `SelectionController` still isn't in the scene, so
  the panel can't be hand-tested end-to-end yet — it only shows/hides correctly today because
  its own `SetSelectedBuilding` was called directly by tests, not because anything in the scene
  is driving it.

- Report 017 (`Docs/Reports/017_gameplay-scene-assembly.md`): **Gameplay scene assembly** —
  the integration feature. Per the human's explicit request, this pass started with a full
  audit of every existing module before touching the scene, and found real gaps rather than
  just wiring what already existed cleanly together:
  - **Units were never actually producible.** The Production Menu only lists buildings; the
    Info Panel's producible-unit icons (Report 016) were built non-interactive; and
    `BuildingDefinition.ProducibleUnits` had no way to know which *prefab* to spawn for a given
    `UnitDefinition`. Re-reading requirements 5/6 together confirmed the Info Panel's
    producible-unit row *is* GI-6's "production sub-menu" — so it needed to be the click target,
    not just a display. Fixed: `BuildingDefinition.ProducibleUnits` is now `List<UnitCatalogEntry>`
    (mirrors `BuildingCatalogEntry`, decisions log #41); `BuildingBase` gained a virtual
    `SpawnPosition` (#42) so resolving "where do this building's products appear" needs no
    per-type cast; `ProducibleUnitIconView` is now clickable and raises the new
    `UnitProductionRequestEventChannel` (`CaseGame.Units`) carrying a `UnitProductionRequest`
    (catalog entry + spawn position, bundled to avoid a circular Units→Selection dependency,
    #43/#44); a new `UnitProductionController` subscribes and spawns via `UnitFactory`.
  - **Two data bugs, found reading the actual asset YAML, not assumed correct.**
    `BuildingDef_Barracks`/`BuildingDef_PowerPlant` still serialized a stale `buildingName`
    field predating the `entityName` rename in Report 010 — `entityName` had been sitting empty
    this whole time, which would have shown blank building names on the Production Menu/Info
    Panel. `UnitDef_Soldier3` had `maxHealth: 1`/`attackDamage: 1` instead of the brief's
    explicit 10 HP / 2 damage (requirement 9). Both fixed via the same asset-touching script,
    documented rather than silently changed (decisions log #46).
  - **UI vs. world input conflict.** `PlacementController`/`SelectionController` read
    `Mouse.current` directly every frame with no awareness of whether the pointer was over a UI
    element — meaning clicking a Production Menu row would *also* fire a world-space
    commit/cancel or select/deselect at whatever was on-screen underneath the panel. Fixed by
    gating both `Update()`s on `EventSystem.current.IsPointerOverGameObject()` (decisions log
    #47) — standard Unity practice for exactly this conflict, not previously needed since no
    controller reading raw pointer input coexisted with clickable UI in the same scene until now.
  - **The actual wiring.** New `CaseGame.Gameplay` module, `GameplayBootstrap` (composition
    root — see decisions log #45 for why this isn't in Core): builds `BuildingFactory`/
    `UnitFactory` with real `--- GAMEPLAY ---` container Transforms and calls
    `PlacementController`/`SelectionController`/`UnitProductionController`'s `Initialize` from
    `Start()`. All three controllers, plus `GameplayBootstrap` itself, now live under a new
    `--- SYSTEMS ---` organizer, fully wired to the real event channel assets from Reports
    014–016. Camera repositioned/resized to frame the grid within the screen area the
    Production Menu/Info Panel don't occlude — computed from `GridDef_Default.asset` and the
    panels' known fixed UI width, **not visually verified** (decisions log #48; flagged for
    hand-test).
  - **Scope check, not scope creep.** Draw-call/batching verification and the Windows build
    export remain separate, later roadmap items — this report is integration and the bugs it
    surfaced, nothing beyond.
  - Every module in the Phase-0 plan is now wired into a live scene; nothing was hand-tested by
    a human yet (the agent cannot enter Play Mode) — see the report's audit checklist for what
    to verify.

- Report 018 (`Docs/Reports/018_draw-call-batching-architecture.md`): **draw-call/batching
  architecture** — requirement 13's "achieved via batching and GPU instancing," the
  architecture half only (the human explicitly asked to defer the *measurement* half until
  after visual polish is further along — see decisions log #49). Audited the actual current
  state rather than assuming: confirmed URP's SRP Batcher was already enabled
  (`Assets/Settings/UniversalRP.asset`, default from project creation, no action needed);
  confirmed every building/unit prefab's `Visuals` `SpriteRenderer` already shares one material
  (URP's default sprite material) — so material-sharing, the other precondition for batching,
  was already correct. The one real gap: no `SpriteAtlas` existed, so even same-material sprites
  from *different* building/unit types (each its own source texture, now the human's imported
  "Tiny Swords" art) would still cost separate draw calls — Unity's sprite batching requires
  matching *texture*, not just material. Added `SpriteAtlas_Gameplay.spriteatlas` covering the
  actual gameplay art folders (`Blue Buildings`/`Blue Units`), packed and verified (145 sprite
  references in the saved atlas). Also enabled GPU Instancing on the project's own
  `M_SpriteGrayscaleGhost` material (was off) — the brief separately names GPU Instancing as a
  required demonstrated pattern; the default URP sprite material is package-shipped and already
  ships instancing-capable, so this project's one custom material was the only thing to change.
  No C# code touched — this was entirely asset/settings configuration, verified by reading the
  generated `.spriteatlas`/`.mat` files back rather than trusting a clean exit code.

- Report 019 (`Docs/Reports/019_camera-controls.md`): landed `CameraControl`'s
  `CameraController` — middle-mouse-drag pan, scroll-wheel zoom (clamped to a designer-tunable
  min/max orthographic size), both skipped while the pointer is over UI. First item off the
  post-hand-test polish backlog (`CURRENT_STATUS.md`, catalogued 2026-07-22) — human-requested
  quality-of-life, not a brief requirement, and deliberately the first pick since it has zero
  dependencies on anything else in the backlog. No `GridModel`/Factory dependency, so — unlike
  every other controller in the project — it needs no `Initialize()` call from
  `GameplayBootstrap`; wired entirely via the Inspector.

- Report 020 (`Docs/Reports/020_placement-grid-architecture-fixes.md`): the first item off the
  post-hand-test polish backlog's "Placement/Grid architecture fixes" group — three related
  fixes, all human-requested (not brief-mandated) but grounded in a real bug the brief's
  requirement 3 (placement) and requirement 12 (destruction) implicitly demand be correct:
  - **Center-anchored footprint placement.** The cell under the cursor is now the footprint's
    *center*, not its bottom-left corner — building art is centered on its own GameObject
    origin, so the old corner-anchoring visibly misaligned the sprite from the cells it actually
    occupied. `GridModel` gained `FootprintCenterToWorld`; `PlacementController` now converts the
    hover cell to a bottom-left "origin" internally (unchanged shape for `IsAreaFree`/
    `SetAreaOccupied`) via integer-division centering (decisions log #52).
  - **"Remove Building"** on the Information Panel. Rather than a parallel removal path, it just
    calls `ApplyDamage(MaxHealth)` on the selected building — reusing the existing death
    pipeline. This surfaced (and fixed) a real pre-existing gap: buildings destroyed in combat
    never released their occupied grid cells either, since nothing previously connected "died"
    to "unoccupy the footprint." Fixed once, for both paths, via a new `GameEntityBase`
    extension point (`protected virtual OnEntityDied()`, decisions log #52) that `BuildingBase`
    overrides. `SelectionController` also gained a defensive stale-reference check (mirrors the
    existing soldier-pruning pattern, decisions log #39) since a selected building can now
    disappear via a second path (decisions log #53).
  - **Unit spawn-cell occupancy.** Units now spawn at the grid cell nearest the Barracks'
    `SpawnPoint` (not its raw world position), and production is blocked outright if that cell
    already has a building *or* another unit on it. `UnitFactory` gained `ActiveUnits` (a live
    set, not a persisted occupancy grid) for the unit-vs-unit check — deliberately scoped to
    spawn-time only, not continuous unit-collision/pathfinding-around-units, which the brief
    never asks for (decisions log #54).
  - `BuildingDef_Barracks`/`BuildingDef_PowerPlant`/the 5 building/soldier prefabs needed no
    data changes — this feature is pure logic, no asset/scene wiring required this time.

- Report 021 (`Docs/Reports/021_building-events-rearchitecture.md`): human-directed correction to
  Report 020's "Remove Building" design. The required event shape is now explicit: Buildings —
  Creation → Event, Removal → Event, Death → Handled by health; Units — Creation → Event, Death
  → Handled by health. Creation was already event-driven for both (Production Menu/Info Panel
  raise a request channel, `PlacementController`/`UnitProductionController` subscribe and call
  their Factory), and Death already went through `ApplyDamage` → `Health` unchanged — only
  Removal was wrong, since decision #53 had folded it into the death pipeline. Fixed by adding
  `BuildingRemovalRequestedEventChannel`: `InfoPanelController.RequestRemoveBuilding` raises it
  instead of calling `ApplyDamage`; `PlacementController` subscribes and calls the new
  `RemoveBuilding` (frees the footprint via `BuildingBase.ReleaseFootprint`, releases the
  instance via `BuildingFactory.Release`) with zero Health involvement; `SelectionController`
  also subscribes to proactively clear a stale selection, since removal no longer sets `IsDead`
  for the old lazy staleness check to catch. `BuildingFactory.Release` also dropped its `prefab`
  parameter (now resolves an instance's pool internally) — manual removal has no "current
  prefab" the way an in-progress placement ghost does. See decisions log #55.

- Report 022 (`Docs/Reports/022_selection-polish.md`): first item off the post-hand-test backlog
  (item 10, "Selection polish"). Replaced `GameEntityBase.SetSelected`'s color-tint approach with
  a real outline: new hand-authored `SpriteSelectionOutline.shader` (neighbor-alpha-sampling
  technique, same style as `SpriteGrayscaleGhost.shader`, decisions log #28) drawn by a new
  `outlineRenderer` child on each of the 5 entity prefabs, toggled via `SpriteRenderer.enabled`
  instead of a color multiply. Prefab wiring (new `Outline` child + field assignment × 5 prefabs)
  done via a throwaway `Scripts/Editor/Setup/Temp/` script, verified by reading the regenerated
  prefab/material YAML back, then deleted per the Editor script policy. See decisions log #56.

- Report 023 (`Docs/Reports/023_movement-timing-fix.md`): item 11 off the backlog, "Movement
  timing fix." `SoldierBase.FollowPath` moved from `Vector3.MoveTowards(..., MoveSpeed *
  Time.deltaTime)` (a constant *world-distance* speed, so a diagonal step took √2× longer than
  an orthogonal one at the same nominal speed) to a fixed per-step duration
  (`StepDuration(moveSpeed) = 1f / moveSpeed`) with time-based interpolation
  (`InterpolateStep`) — every step, orthogonal or diagonal, now takes the same time, since
  `MoveSpeed` means cells/second and every step is exactly 1 cell. Both helpers are pure static
  methods so the actual timing/interpolation contract is directly unit-tested without a live
  coroutine/Update loop (the coroutine itself remains Play-Mode-only to hand-verify). See
  decisions log #57.

- Report 024 (`Docs/Reports/024_info-panel-grid-layout.md`): item 13 off the backlog, "Info
  Panel producible-units layout fix." `ProducibleUnitsContainer`'s `HorizontalLayoutGroup` (no
  wrapping — overflowed sideways past 3 entries) replaced with a `GridLayoutGroup` (Fixed Column
  Count, 3 by default) plus a `ContentSizeFitter` (vertical Preferred Size, so the container
  grows to fit however many rows are actually needed) on
  `Assets/_Project/Prefabs/UI/InformationPanel.prefab`. Pure built-in-component wiring — no code
  change, since neither `InfoPanelController` nor `ProducibleUnitIconView` know or care how their
  container arranges children. Human explicitly asked for the grid shape/size to be "adjustable
  through the editor" — `GridLayoutGroup`'s own Inspector (Cell Size, Spacing, Constraint,
  Constraint Count) already provides exactly this, so no custom layout script was needed. Done
  via a throwaway `Scripts/Editor/Setup/Temp/` script (component swap, not a single-property
  tweak), verified by reading the regenerated prefab YAML back, then deleted. See decisions
  log #58.

- Report 025 (`Docs/Reports/025_info-panel-stack-reflow.md`): same-day polish-and-bugfix pass
  following Report 024, in three parts:
  - Added a `VerticalLayoutGroup` to `InformationPanel.prefab`'s `PanelContent` so its 4 children
    (`BuildingIcon`, `BuildingName`, `ProducibleUnitsContainer`, `RemoveButton`) stack
    top-to-bottom instead of each being independently/fixed-anchored — `RemoveButton` was
    previously anchored to a fixed position regardless of the producible-units grid's actual
    height, so it could overlap the grid once it grew past 1 row.
  - **Bug found by the human**: the initial version left `Force Expand Width` on, which
    (per `HorizontalOrVerticalLayoutGroup`'s own source) forces *every* child's flexible width
    to at least 1 regardless of its own `LayoutElement` — so `BuildingIcon`/`RemoveButton` got
    stretched to the full panel width despite their `preferredWidth`. Fixed (by the human,
    directly in the Editor) by turning `Force Expand Width` off and instead giving
    `BuildingName`/`ProducibleUnitsContainer` an explicit `LayoutElement.flexibleWidth = 1`, so
    only those two request the extra space. See decisions log #59 (corrected to match).
  - **Second bug found by the human**: selecting a building showed the *previous* selection's
    stale panel layout for one frame, only correcting itself on the next selection change — and
    switching straight from a building with more producible units to one with fewer (Barracks'
    8 to Power Plant's 0) never corrected at all without deselecting first. Root cause was
    two-layered: Unity's layout groups rebuild lazily by default (fixed by forcing
    `LayoutRebuilder.ForceRebuildLayoutImmediate` in `SetSelectedBuilding`), and `Destroy()` in
    Play Mode defers actual removal to end-of-frame, so a forced rebuild still measured
    about-to-be-destroyed old icons as real children when *shrinking* (fixed by switching
    `DestroyView` to `DestroyImmediate` unconditionally). See decisions log #60.

- Report 026 (`Docs/Reports/026_ui-visual-polish.md`): backlog item 14, "UI visual polish."
  Two independent pieces, both pure editor/prefab/scene wiring (one small, low-risk code change
  for the second piece):
  - **Banner headers** on the Production Menu and Information Panel (not the Game Board, per
    the human's original ask) — `ProductionMenu.prefab`'s root anchor trimmed to free a strip at
    the top for a `ProductionMenuBanner` (Canvas-level sibling in `Gameplay.unity`); a static
    "Information" banner added as the first element of `InformationPanel.prefab`'s existing
    `VerticalLayoutGroup` stack (Report 025) instead of restructuring `panelRoot`'s show/hide
    semantics.
  - **Main Menu "How to Play" screen** — `MainMenuController` gained a third panel (`howToPlayPanel`/
    `howToPlayButton`/`howToPlayBackButton`), mirroring the existing Settings button/panel/
    back-button pattern exactly. Built by duplicating the Settings button/panel in
    `MainMenu.unity` (safer than hand-authoring new TMP/Button hierarchies) and stripping the
    Settings-specific children plus the cloned `SettingsController` component, which would
    otherwise `NullReferenceException` on `OnEnable` once its dropdown/toggle/button children
    were removed.
  Done via a throwaway `Scripts/Editor/Setup/Temp/` script (structural, multi-object scene/prefab
  changes across 2 scenes + 1 prefab), verified by reading the regenerated files back. Per
  explicit instruction, no dedicated batchmode compile/test pass was run this turn — though the
  script's own successful execution already required Unity to compile
  `MainMenuController.cs`'s changes first, so that much is incidentally confirmed. See decisions
  log #61.

- Report 027 (`Docs/Reports/027_ranged-combat-overhaul.md`): backlog item 12, "Ranged combat &
  combat overhaul" — the largest single feature this project, folding in the human's detailed
  follow-up spec (see `CURRENT_STATUS.md` item 12). `UnitDefinition` gains `Ranged`/`AttackRange`/
  `AttackSpeed`. `SoldierBase.TryAttack` (a single instant hit, decision #37) is replaced by
  `Attack(target, grid, projectileFactory)`: a sustained tick loop (once every
  `1 / AttackSpeed` seconds) that walks into `AttackRange` first via the new
  `AStarPathfinder.FindApproachCell` if needed, then continues until the target dies or leaves
  range — melee and ranged share this exact path, only `PerformAttack`'s damage delivery differs
  (instant for melee, a launched `Projectile` for ranged). `MoveTo`/`Attack` now share one
  `_actionCoroutine` slot (a move order cancels an attack and vice versa; a new `Attack` call on
  a different target switches immediately, human-confirmed). `SelectionController.HandleRightClick`
  now takes `GameEntityBase` (was `IDamageable`) so it can pass the target straight through, and
  gained a `ProjectileFactory` dependency alongside `GridModel`. Soldier 2 became the Archer
  (`ranged: true`, `attackRange: 4`, existing sprite/damage unchanged). New `Projectile`/
  `ProjectileFactory` (`CaseGame.Units`) — a pooled, purely-visual homing indicator with no
  collider, tracking its target's live position every frame. While verifying, caught and fixed a
  real pre-existing bug unrelated to this feature: `InfoPanelControllerTests`' `_panelRoot` was a
  plain `GameObject` (no `RectTransform`), so Report 025's
  `LayoutRebuilder.ForceRebuildLayoutImmediate((RectTransform)panelRoot.transform)` cast threw —
  never caught before since that report's turn skipped batch testing. See decisions log #62.

- [ ] 1. Unity 2021 LTS, 2D, Windows build
- [ ] 2. Production Menu: Barracks, Power Plant, Soldier Units (+ extensible for more)
- [ ] 3. Building placement with invalid-area feedback; name/image/dimensions
- [ ] 4. Free, instant, unlimited production
- [ ] 5. Info Panel shows selected building image + producible unit images
- [ ] 6. Only Barracks produce units
- [ ] 7. Barracks spawn point for produced soldiers
- [ ] 8. Left-click select / right-click move; shortest path; routes around buildings
- [ ] 9. 3 soldier types, 10 HP each, damage 10/5/2
- [ ] 10. Building HP: Barracks 100, Power Plant 50
- [ ] 11. Right-click attack on unit/building
- [ ] 12. Destruction at 0 HP
- [ ] 13. Draw calls < 20 via batching/instancing
- [ ] 14. Multi-aspect-ratio/resolution support
- [ ] 15. Production Menu (pooled infinite scroll) / Game Board / Information Panel layout
- [ ] 16. Legible, standards-compliant code
- [ ] 17. Evaluation-grade scene/naming/folder hygiene
- [ ] 18. Edge cases considered
- [x] 19. Main Menu with Play button + Settings screen (resolution/display-mode) — Report 005, `Assets/_Project/Scenes/MainMenu.unity`; human hand-tested and confirmed working (2026-07-21)

## 6. Decisions log

| # | Decision | Reason | Alternatives rejected |
|---|---|---|---|
| 1 | Use a Singleton for `GameManager` (isolated behind a minimal interface where consumed) | Brief's DESIGN section explicitly names Singleton as a required pattern, overriding CONVENTIONS.md's baseline anti-singleton stance (golden rule 1: brief overrides best practice) | Pure DI/service locator — not what was asked for; brief names Singleton specifically |
| 10 | `GameManager` implemented (Report 002) with static instance isolated behind `IGameManager`; duplicate-instance self-destruction uses `Application.isPlaying` to pick `Destroy` (Play Mode) vs `DestroyImmediate` (Edit Mode) | Fulfils decision #1's "isolate behind a minimal interface" clause; the Play/Edit Mode branch is needed because `Destroy()` is invalid outside Play Mode | A single unconditional `Destroy()` call — throws in Edit Mode |
| 11 | Dropped automated EditMode tests for `GameManager`'s Awake-driven singleton/lifecycle behavior; hand-test instead | Empirically confirmed (via temporary `Debug.Log` instrumentation) that `Awake` does not fire on `AddComponent`-created objects within this machine's `-runTests -testPlatform EditMode` batchmode run, even after 10 `yield return null` frames in a `[UnityTest]` — an environment limitation, not a logic bug. Documented in `ENVIRONMENT.md` for future features | Continuing to chase the EditMode test (would test the environment, not the code); PlayMode tests (not attempted — out of scope for this narrow feature) |
| 2 | MVC-style split: SO/plain-C# Models, MonoBehaviour Views, MonoBehaviour Controllers wiring via Events | Brief explicitly requires "UI and Logic separated... using techniques like MVC" | Fully custom architecture ignoring the brief's named technique |
| 3 | Custom grid-based A* pathfinding, no Unity NavMesh/NavMesh2D | Brief explicitly names "Pathfinding (A*)" as the required algorithm; NavMesh is never mentioned and would add an unrequested package (golden rule 6) | Unity NavMesh 2D package |
| 4 | Object pooling used for both the infinite Production Menu scroll view AND for units/buildings that are frequently created/destroyed | Brief explicitly ties Object Pooling to the infinite scroll view (UX section) and separately lists Object Pooling as a required general pattern; frequent Instantiate/Destroy of units/buildings also directly threatens the <20 draw call budget (requirement 13) | Plain Instantiate/Destroy everywhere |
| 5 | Game Board modeled as a discrete grid; cell size lives on a `GridDefinition` SO and building/unit footprints live on their own `Definition` SOs — **none of these are hardcoded**, all designer-editable | Human confirmed grid layout from the UI mockup, then corrected that cell size is "any X by X pixel value" decided once visuals are chosen (not the mockup's 32×32px), and footprints "should be easily modifiable by a designer." Mockup's drawn row/column count remains illustrative only — actual board dimensions are a data/visual-fit choice | Continuous/free-form placement with custom collider-based validity checks; hardcoding 32×32px or the mockup's footprint numbers as constants |
| 6 | No resource/currency economy implemented | Brief requirement 4 explicitly states production has no time cost and is unlimited; no cost currency is mentioned anywhere | Adding a generic resource system speculatively |
| 7 | No enemy AI / opposing faction implemented, human-confirmed | Human explicitly confirmed no opposing AI faction; combat requirement (10) only specifies right-click-to-attack a unit/building, any legal target | Building a full enemy-faction AI (would have been speculative scope per golden rule 2) |
| 8 | Building/unit system kept modular across every layer: data (SO defs), creation (Factories), and UI (Production Menu iterates definitions generically; producible-unit lists are data on `BuildingDefinition`, not switch-cased) — even though only Barracks + Power Plant + 3 soldiers ship | Human explicitly required extensibility beyond just the data layer: "all systems for buildings, including UI as well as the list of units it can spawn etc. should be modular and extensible" | Hardcoding a fixed enum/switch of building types in the Production Menu UI or elsewhere |
| 9 | Added a Main Menu (`MainMenu.unity`) with Play + Settings (resolution/display-mode), inserted into scene flow as `Boot` → `MainMenu` → `Gameplay` | Human-mandated so the evaluator can exercise GI-13 (aspect ratio/resolution support) directly rather than only via editor Game-view resizing; brief's own resolution requirement is the stated reason | No main menu (original Phase-0 assumption, since superseded) |
| 12 | `GameManager.Awake` calls `DontDestroyOnLoad(transform.root.gameObject)` instead of `DontDestroyOnLoad(gameObject)` (Report 003) | `DontDestroyOnLoad` only accepts root GameObjects; `GameManager` is a child of `--- SYSTEMS ---` per CONVENTIONS.md scene composition, so the un-rooted call threw at runtime (human caught this hand-testing Report 002). Persisting the whole root also matches ARCHITECTURE.md §4's intent that all of `--- SYSTEMS ---` (event channels, pools, etc., once they exist) survive scene loads together | Moving `GameManager` to scene root instead — would violate CONVENTIONS.md's "no loose objects at root" rule |
| 13 | Added `SceneReference` (`CaseGame.Core`) as a reusable asset-picker wrapper for scene references, backed by an editor-only `SceneAsset` field synced to a runtime-safe `sceneName` string via `ISerializationCallbackReceiver`; added `SceneReferenceDrawer` so it still shows as a single object-picker field, not two raw fields. First script under `Scripts/Editor/`, first `CaseGame.Editor` asmdef (Report 004) | Human-mandated project convention: prefer typed asset-picker `SerializeField`s over raw strings for asset references, to avoid typo-prone/rename-unsafe hardcoded names — `GameManager.firstScene` was the first offender | Leaving `firstSceneName` as a raw string; using Unity's Build-Settings scene index instead (more fragile to reordering) |
| 14 | Relocated `Boot.unity`/`Gameplay.unity` from `Assets/Scenes/` to `Assets/_Project/Scenes/` (Report 005) | Both had been hand-created in Unity's default `Assets/Scenes/` folder during earlier editor hookup checklists instead of the CONVENTIONS.md-mandated `_Project/` root; caught by the agent while starting the Main Menu feature, fixed with explicit human approval since it required moving/editing tracked assets and `ProjectSettings/EditorBuildSettings.asset` | Leaving them misplaced (would fail the brief's explicit folder-structure evaluation criterion, GI-15) |
| 15 | Main Menu built with TextMeshPro (`TMP_Dropdown` for resolution selection, via `TMPro.TMP_DefaultControls` factory methods) and the Input System package's `InputSystemUIInputModule` on the scene's `EventSystem` (Report 005, redone in place) | Human explicitly installed `com.unity.inputsystem` and imported TMP Essential Resources specifically for this feature, with Input System also intended for gameplay input later (per CONVENTIONS.md's pre-existing baseline default, previously un-actionable because the package wasn't installed) and TMP_Dropdown specifically requested over the earlier prev/next-button cycler workaround | Keeping the legacy `UnityEngine.UI`/cycler version from the original pass at this feature — superseded, not shipped |
| 16 | `TMP_DefaultControls.CreateButton/CreateText/CreateDropdown(new TMP_DefaultControls.Resources())` (all-null sprite fields) safely builds fully-functional TMP widgets — including `TMP_Dropdown`'s full template/viewport/scrollbar sub-hierarchy — via editor script now that TMP Essentials are imported; `InputSystemUIInputModule` self-configures via its `Reset()`/`OnEnable()`'s `AssignDefaultActions()`, wiring a complete default Point/Click/Navigate/Submit/Cancel action set from the Input System package's own bundled default actions asset, with no manual `.inputactions` authoring needed (Report 005) | Empirically verified by reading both source (`Library/PackageCache/...`) and the generated scene YAML back after running the setup script — confirms these APIs are safe to drive from a headless batchmode editor script on this pinned version | Hand-building the Dropdown's template hierarchy or a custom `InputActionAsset` — unnecessary, both packages already provide safe, complete defaults |
| 17 | Events module uses ScriptableObject event channels (`GameEvent`, generic `GameEventChannel<T>` base), not plain static C# `event`/`Action` fields, plus a `GameEventListener` MonoBehaviour bridging to a designer-configured `UnityEvent` (Report 006) | CONVENTIONS.md's baseline already names "ScriptableObject event channels when designer-facing wiring is valuable" as the preferred decoupling mechanism; SO channels let a raiser and listener both reference the same asset with zero direct-reference or static coupling, and are asset-picker-friendly (consistent with decision #13's asset-picker-over-string convention). Only the parameterless `GameEvent` ships now — typed payload channels (e.g. a future `BuildingDefinition` channel for Selection → Info Panel) get added as concrete `GameEventChannel<T>` subclasses alongside whichever feature first needs one, since inventing payload types speculatively would violate golden rule 2 | Static C# `event Action` fields on a static bus class (works, but re-introduces static/global coupling the brief's Events requirement is meant to avoid); building typed listener/channel variants now for hypothetical future payloads |
| 18 | `PrefabPool<T>` wraps Unity's built-in `UnityEngine.Pool.ObjectPool<T>` rather than a hand-rolled stack/queue pool (Report 007) | The brief mandates the Object Pooling *pattern* being demonstrably used, not a from-scratch implementation; `ObjectPool<T>` has been part of `UnityEngine.CoreModule` since Unity 2021.1 (confirmed available on the pinned `2021.3.45f2` by compile — no package needed) and already handles capacity limits and double-release detection (`collectionCheck: true`) correctly, which a hand-rolled version would have to reimplement and re-test | Writing a custom pool from scratch (more code to test/maintain for no benefit — golden rule 1: where the brief is silent on *how*, use modern Unity best practice) |
| 19 | `Health.Damaged`/`Health.Died` are plain per-instance C# events, not a shared `GameEventChannel<T>` from the Events module (Report 008) — refines the Phase-0 module map, which listed Combat as depending on Events | Every unit/building will own its *own* `Health` instance; a shared/global SO channel would broadcast every instance's HP changes to every listener, which is the wrong shape for "this specific soldier took damage." Plain instance events are the correct decoupling tool here (raiser and the one interested listener — that instance's own controller/view — share the `Health` object reference directly), while a global channel remains right for genuinely cross-system, one-of-a-kind signals (e.g. a future `SelectionChanged`) | Routing every `Health` instance's events through a shared `GameEventChannel<Health>` — would require every listener to filter for "is this event about the instance I care about", solving a problem that doesn't exist here |
| 20 | Added a minimal `UnitDefinition` (SO, data only: name/sprite/footprint/HP/attack damage) under `CaseGame.Units` as part of the Buildings feature, ahead of the full Units feature (Report 009) | Brief requirement 2 explicitly mandates `BuildingDefinition` reference a list of `UnitDefinition`s (data-driven producible units, not switch-cased) — `BuildingDefinition` can't compile without the type existing. This is a data-only stub, not scope creep into Units' actual behavior (`SoldierBase`/`Soldier1/2/3`/`UnitFactory` still land as their own feature) | Making `BuildingDefinition`'s producible list untyped (`object`/string names) to defer the dependency — would give up compile-time safety and contradict the brief's explicit `UnitDefinition` reference mandate |
| 21 | `BuildingBase.Initialize(definition, onDied)` takes an optional death callback instead of knowing about `PrefabPool<T>`/pooling itself; `BuildingFactory.Create` wires `() => pool.Release(instance)` as that callback | Keeps `BuildingBase` single-responsibility (HP/sprite only) and testable via a plain callback with no pool in the test at all; the Factory is the one thing that knows both "this instance came from a pool" and "this instance died," so it's the natural place to connect the two | `BuildingBase` holding a direct reference to its own pool (couples the humble building class to a pooling implementation detail it doesn't need to know about) |
| 22 | Extracted `GameEntityDefinition`/`GameEntityBase` (new `CaseGame.Entities` module) as a shared base for `BuildingDefinition`/`BuildingBase` and (future) `UnitDefinition`/`SoldierBase` (Report 010) | Human-suggested. `BuildingDefinition`/`UnitDefinition` already duplicated identical fields (name, sprite, footprint, maxHealth) and `BuildingBase`'s `Health`/`IDamageable`/sprite/death-callback logic is exactly what a future `SoldierBase` needs too — real, current duplication, not speculative. Gives the brief's mandated "Inheritance" a real hierarchy rather than the shallow `Barracks`/`PowerPlant : BuildingBase` split alone. Named `GameEntity*`, not `Unit*` — the brief's own text uses "unit(s)" specifically to mean soldiers throughout ("Soldier Units," "attack...on a unit or building," etc.), always as a sibling of "building," never as an umbrella term, so naming the shared base "Unit" would misread against the brief's own vocabulary and would have collided with the already-existing `UnitDefinition` | Naming the shared base `Unit*`/`UnitBase` (collides with existing `UnitDefinition` and the brief's own soldier-specific use of "unit"); leaving the duplication in place since only 2 concrete types ship |
| 23 | `AStarPathfinder` is a static, stateless class (`FindPath(grid, start, goal)`) rather than an instantiable object holding the grid as a field (Report 011) | It has no state beyond the grid reference passed in for a single call — no per-instance data to justify object lifecycle management; callers don't need to construct/hold a pathfinder object at all | Instance-based `new AStarPathfinder(grid).FindPath(start, goal)` — adds ceremony with no benefit here |
| 24 | 8-directional movement (diagonal cost √2, octile-distance heuristic) with corner-cutting prevention, instead of 4-directional-only | 4-directional-only would still technically "route around" buildings but produce visibly blocky, unnatural-looking paths for a 2D top-down game; corner-cutting prevention (reject a diagonal step if either flanking orthogonal cell is blocked) is what actually makes "wander around the buildings" (GI-7/8) true instead of soldiers visually clipping through a building's corner | 4-directional-only movement (simpler, but worse-looking paths and doesn't need the corner-cut edge case at all — would undersell "accounting for edge cases," GI-16) |
| 25 | No coroutine-based path-request queue/manager in this pass, despite ARCHITECTURE.md's original module description mentioning "path requests" (Report 011) | Nothing calls `AStarPathfinder` yet — Units/Selection (the actual callers) don't exist. Building a request-queue layer now would be speculative (golden rule 2); the brief's Coroutine requirement doesn't specifically require it live in Pathfinding — it's equally at home in the future unit-movement code that steps a soldier along the returned path | Building an async/queued path-request layer now on the assumption it'll be needed — defer until a real caller shows whether pathfinding actually needs to be spread across frames |
| 26 | The brief's 3 soldier types ship as 3 `UnitDefinition` assets/prefabs sharing one concrete `Soldier : SoldierBase` class, not 3 separate `Soldier1`/`Soldier2`/`Soldier3` classes as Phase-0's module map assumed (Report 012) | Requirements 9's only difference between the 3 types is attack damage (10/5/2) — a data value. Nothing behavioral differs, so 3 empty subclasses would be an OOP checkbox with no substance behind it, and would contradict the project's own established philosophy of differentiating via data rather than code (the same reasoning already applied to `BuildingDefinition`'s producible-units list, decision #8). `SoldierBase` stays abstract (mirroring `BuildingBase`) so a *genuinely* different future soldier type still has a clean extension point | 3 empty subclasses purely to match the Phase-0 naming — would look like more inheritance than is actually true, and the brief never requires the classes to be literally named Soldier1/2/3, just that 3 soldier types with those stats exist |
| 27 | `SoldierBase.MoveTo` requests a path and walks it via `StartCoroutine` directly on the soldier itself, rather than Selection owning movement execution (Report 012) | Matches ARCHITECTURE.md §3's own data-flow description: "the soldier's controller consumes it, requests a path from Pathfinding, and moves along it via coroutine." Selection's job (once built) is just to interpret the right-click and call `MoveTo`/`TryAttack` — the humble MonoBehaviour that owns the transform is the natural place for the coroutine that moves it | Movement execution living in Selection instead — would make Selection reach into a soldier's transform from outside, and scatters "how a soldier moves" across two modules for no benefit |
| 28 | Placement ghost is desaturated first (custom `Art/Shaders/SpriteGrayscaleGhost.shader`, luminance per-pixel), *then* tinted via `SpriteRenderer.color`, rather than tinting the building's full-color sprite directly (Report 013) | Human-directed. A plain color multiply over the original full-color sprite would blend with whatever hues are already in the art (e.g. a blue banner region would go muddy dark-green instead of reading as clean green) — desaturating first is what makes the green/red read as an unambiguous valid/invalid signal (GI-3's "user must be visually informed when the location is invalid"). Hand-authored HLSL, not Shader Graph, to avoid any version-availability uncertainty around URP's 2D Shader Graph target on this pinned version (golden rule 7); verified by batchmode import producing no shader compile errors | Tinting the full-color sprite directly (simpler, but muddy/ambiguous); a Shader Graph 2D Unlit target (unverified availability on this exact URP/Editor version, unnecessary risk for a ~25-line shader); pre-baking desaturated texture variants as separate art assets (extra asset-authoring burden per building type, redundant with what a shader does automatically for any sprite) |
| 29 | The ghost preview and the final placed building are the *same* pooled instance — `BuildingGhostView` toggles `Visuals`/`VisualsGrayscale`/`Hitbox` active state on one object — rather than a separate temporary ghost object swapped for a freshly-created real one on commit (Report 013) | Human-directed. Avoids managing two objects with two lifecycles for what is really one placement lifecycle; reuses the existing pooled `BuildingFactory.Create` path unchanged — cancelling just returns the same instance to its pool (new `BuildingFactory.Release`) instead of needing separate ghost-cleanup logic | A disposable non-pooled ghost prefab instantiated fresh each placement attempt and destroyed on cancel/commit — works, but duplicates object-management logic and doesn't reuse pooling for the ghost phase |
| 30 | `PlacementController`'s decision logic (`BeginPlacement`/`UpdateGhostAt(cell)`/`TryCommitAt(cell)`/`CancelPlacement`) is exposed as plain methods taking an explicit cell, with `Update()` reduced to a 5-line mouse-reading orchestrator that calls them (Report 013) | Keeps the actual placement logic directly testable (no Mouse/Update-loop dependency in the tests) while `Update()` stays humble per CONVENTIONS.md — the same "extract the testable decision, keep the MonoBehaviour thin" pattern used throughout the project | Putting mouse-reading and validity/commit logic together inside `Update()` — untestable without simulating Input System device state and frame ticks |
| 31 | The Production Menu's recycling decision (which catalog index each pooled row slot shows, and at what Y position, given a scroll offset) is extracted into `ScrollRecycler` — a static, plain-C# class with no `ScrollRect`/UI dependency (Report 014) | Same "extract the testable decision" pattern as #30, applied to the trickiest new logic in the project so far. `ScrollRect`'s exact `anchoredPosition` sign convention isn't something the agent can verify without an interactive Play Mode session, but the *recycling math itself* (given a scroll offset, which items are visible) is pure and fully testable independent of that uncertainty | Writing the recycling logic directly inside `ProductionMenuController`'s `ScrollRect.onValueChanged` callback — untestable without a live `ScrollRect`/Canvas, and conflates "what Unity's scroll callback reports" with "what should be visible", the same two concerns #30 already separated for Placement |
| 32 | `BuildingCatalogEntry` (struct: `BuildingDefinition` + prefab pair), `BuildingCatalog` (SO listing them), and `BuildingCatalogEntryEventChannel` (typed event channel carrying one) all live in `CaseGame.Buildings`, not `CaseGame.UI.Production` (Report 014) | This is Buildings-domain data — "a building type and the prefab to spawn for it" — with nothing UI-specific about it. Both consumers (`UI.Production`, which lists them, and `Placement`, which subscribes to the channel) already depend on `Buildings`, so keeping the type there avoids introducing a *new* cross-module dependency in either direction | Defining these types in `UI.Production` (would make `Placement` depend on `UI.Production` just to receive its event payload type — backwards, since Placement is the more foundational module) |
| 33 | `PlacementController` subscribes directly to `BuildingCatalogEntryEventChannel` in its own `OnEnable`/`OnDisable` and forwards to its existing `BeginPlacement` (Report 014), rather than a separate bridge/listener component | "Start placing what was requested" is squarely `PlacementController`'s own responsibility — no different in kind from being called directly, just triggered by an event instead. This is also the project's *first* concrete `GameEventChannel<T>` payload channel, landing exactly when decision #17 said one would: alongside the feature that first needs it | A generic bridge component (`GameEventListener`-style) forwarding to a `UnityEvent` — that pattern exists for wiring a *designer-configured* response in the Inspector to a parameterless `GameEvent`; here the response (`BeginPlacement(entry.Definition, entry.Prefab)`) is fixed, non-designer-facing logic, so a direct subscription is simpler and just as decoupled |
| 34 | `ProductionMenuController.poolSize` is a fixed `[SerializeField] int` (default 8) rather than computed at runtime from the `ScrollRect` viewport's `RectTransform.rect.height` (Report 014) | Reading `RectTransform.rect` reliably requires a layout pass to have already run, which isn't guaranteed at `Awake`/`Start` for a freshly-loaded UI hierarchy — a real Unity UI timing hazard the agent can't fully verify without an interactive session. A designer-set pool size sidesteps that fragility entirely; the brief's actual data set (2 buildings today, a handful more at most) makes "exactly enough slots for the current viewport" an unnecessary optimization anyway — the point being demonstrated is the pooling/recycling *technique*, not squeezing out the last unused row view | Computing `poolSize` from `scrollRect.viewport.rect.height / itemHeight` at `Awake` — more "automatically correct," but fragile to a real Unity layout-timing gotcha with no way for the agent to verify it here; flagged for the human to double-check scroll behavior visually once in the Editor |
| 35 | Production Menu UI (`--- UI ---` organizer, `Canvas`/`EventSystem`, the `ProductionMenu` scroll view, `ProductionMenuItem` prefab) was added to `Gameplay.unity` as part of *this* feature, but `PlacementController` was deliberately *not* added to the scene (Report 014) | The Production Menu's own UI is this feature's actual scope — same as how Main Menu's feature built its own scene UI. `PlacementController` still has nothing to be initialized with (`Initialize(grid, factory)` needs a live `GridModel`/`BuildingFactory`, and no scene bootstrap wiring those exists yet) — adding it now would mean either leaving it broken or building bootstrap plumbing that's `Gameplay scene assembly`'s job, not this feature's | Also wiring `PlacementController` + a minimal bootstrap now, to make the whole produce→ghost→place loop hand-testable today — would have front-run Gameplay scene assembly's actual scope (Info Panel, camera framing, full 3-area layout) for the sake of one extra hand-test, when this feature's logic is already fully covered by automated tests |
| 36 | Multi-selecting soldiers uses shift-click (add/remove one at a time), not a drag/marquee box-select (Report 015) | The brief's only signal on this is the wording "unit(s)" in requirement 8 — it doesn't specify a mechanism. A drag-select needs a new visual (a selection-rectangle overlay) and world-rect-vs-position geometry; shift-click needs neither, is a completely standard alternative multi-select convention, and keeps the feature's diff to logic that's directly unit-testable (no `ScrollRect`-style Editor-verification-only visual). Recorded as an explicit interpretation, not a silent guess, so it's easy to revisit if a drag-box is actually wanted | Drag/marquee box-select (more "expected RTS feel," but a materially bigger feature — a rectangle-overlay View, screen-to-world rect conversion, and a visual the agent can't verify without an interactive session, unlike the rest of this feature) |
| 37 | Right-click attack (`SoldierBase.TryAttack`) is instant and range-less — no check that the target is within any distance of the attacking soldier, no "walk into range first" (Report 015) | `TryAttack`'s existing signature (Report 012) already only takes an `IDamageable`, no position/range info, and its doc comment explicitly anticipated Selection wiring it up exactly this way. The brief's requirement 11 says only "attack via right-click on a target," with no range/approach language, unlike requirement 8's explicit "shortest path" for movement — so range enforcement would be adding a mechanic the brief never asked for | Requiring the soldier to `MoveTo` attack range before applying damage — a materially bigger feature (attack-range data, movement-then-attack sequencing) with no textual basis in the brief |
| 38 | `GameEntityBase.Initialize` now resets `spriteRenderer.color` to white every call (Report 015) | Necessary correctness fix, not a stylistic addition: `SetSelected` (this report) makes sprite color stateful, and instances are pooled/reused (`BuildingFactory`/`UnitFactory`) — without the reset, an instance released while selected would start its *next* life pre-tinted yellow, a real bug the moment Selection exists, not a hypothetical one | Leaving color-reset to whoever adds selection visuals to a *specific* type later — would silently reintroduce the bug for every pooled type, since the root cause (pooling + stateful color) lives in the shared base |
| 39 | `SelectionController.HandleRightClick` prunes dead/null soldiers from the selection (`_selectedSoldiers.RemoveAll(s => s == null \|\| s.IsDead)`) at the point of use, rather than proactively unsubscribing from a death event as soon as a soldier is deselected-by-dying (Report 015) | Handles the realistic case (a selected soldier dies, sits inactive in its pool, hasn't been reused yet) with one line and no event-subscription bookkeeping. The narrower race — a dead selected soldier's pooled instance gets reused as a *different* soldier before the player's next right-click — is a known, accepted, documented limitation, not silently ignored: pruning by `IsDead` can't catch a reused instance still passing sanity as "not dead" | Subscribing to each selected soldier's death individually (would need a per-instance-death event GameEntityBase doesn't currently expose, plus careful subscribe/unsubscribe pairing on every selection change) to close a race that's unlikely to matter for a demo project of this scope |
| 40 | `InfoPanelController`'s producible-unit icons are plain `Instantiate`/`Destroy` per selection change, not pooled via `PrefabPool<T>` (Report 016) | The brief ties Object Pooling specifically to the Production Menu's *infinite* scroll view (UX section) and to frequently spawned/destroyed units/buildings (decision #4) — this list is always small (≤3 today, bounded by however many `UnitDefinition`s a `BuildingDefinition` lists) and only rebuilds on the rare "selection changed" event, not every frame or even every second. Pooling it would need an arbitrary pool-size guess with no data-driven contract behind it, for a churn rate pooling doesn't meaningfully help | Reusing `PrefabPool<ProducibleUnitIconView>` for consistency with `ProductionMenuController` — would apply the same tool to a problem it doesn't have (over-engineering, golden rule 2) |
| 41 | `BuildingDefinition.ProducibleUnits` changed from `List<UnitDefinition>` to `List<UnitCatalogEntry>` (a `UnitDefinition` + `SoldierBase` prefab pair, mirroring `BuildingCatalogEntry`) (Report 017) | Nothing anywhere in the project could answer "which prefab do I instantiate for this producible unit" — `UnitFactory.Create` needs both a definition and a prefab, but `BuildingDefinition` only ever stored the definition. Confirmed safe to change (not just theoretically cleaner): both `BuildingDef_Barracks`/`BuildingDef_PowerPlant`'s `producibleUnits` were still `[]` (empty) when this was found, so no authored data was lost | A separate global `UnitCatalog` registry (mirroring `BuildingCatalog`) that `InfoPanelController` looks up prefabs from — redundant, since `BuildingDefinition.ProducibleUnits` is already the complete, per-building list this problem needs; a global catalog would just be a second place the same definitions have to be kept in sync |
| 42 | `BuildingBase` gained a `public virtual Vector3 SpawnPosition => transform.position`; `Barracks` now *overrides* it (was previously its own unrelated property) (Report 017) | `InfoPanelController`/`UnitProductionController` need "where do this building's products spawn" for *any* building capable of producing units — hardcoding a cast to `Barracks` there would violate the project's own established no-per-type-branch discipline (decisions #8/#32/#35's data-driven-generic thread) the moment a second unit-producing building type ever exists. Polymorphism is literally the brief-mandated tool for exactly this ("OOP: Polymorphism, Inheritance") | Type-checking/casting to `Barracks` wherever a spawn position is needed — works today (only Barracks produces units) but bakes in a per-type branch the rest of the project deliberately avoids |
| 43 | `UnitProductionRequest` (plain struct: a `UnitCatalogEntry` + a `Vector3` spawn position) bundles the spawn position directly, rather than `UnitProductionController` looking up "whichever building is currently selected" itself (Report 017) | `UnitProductionController` (`CaseGame.Units`) would otherwise need a reference to `SelectionController` (`CaseGame.Selection`) to answer "where does this spawn" — but `Selection` already depends on `Units`, so that would be a circular module dependency. `InfoPanelController` already knows which building's row is being displayed when it binds each icon, so it's the natural place to supply the spawn position once, at bind time | `UnitProductionController` holding a `SelectionController` reference and reading `SelectedBuilding.SpawnPosition` at request time — circular dependency, and also subtly wrong if the selection changed between the icon being shown and being clicked |
| 44 | `ProducibleUnitIconView` is now clickable (added a `Button`, raises `UnitProductionRequestEventChannel`) — reversing Report 016's explicit "purely informational, no button" design | Re-auditing requirements 5 and 6 together while assembling the full scene: the Production Menu only ever lists *buildings* (confirmed by `BuildingCatalog`'s actual content and `ARCHITECTURE.md`'s own module description), so the Info Panel's producible-unit row is the *only* place a unit is ever shown to the player — meaning it **must** be GI-6's "production sub-menu," not a passive display, or units could never be produced at all. This was a real, previously-unnoticed gap: nothing in the shipped code could ever spawn a soldier | Leaving it non-interactive and inventing some other production path (e.g. a second scroll view for units) — would contradict GI-6's own wording ("sub-menu") and duplicate UI.Production's already-built pooled-scroll machinery for a list that's always small, non-infinite, and doesn't need it |
| 45 | New `CaseGame.Gameplay` module holding just `GameplayBootstrap`, rather than adding it to `CaseGame.Core` (Report 017) | `GameManager`/`SceneReference`/`IGameManager` are generic, reusable across *any* scene (Boot, MainMenu, Gameplay); `GameplayBootstrap` is specifically Gameplay.unity's own composition root, wiring Grid/Buildings/Units/Placement/Selection together — a fundamentally different, scene-specific concern that would otherwise dilute what "Core" means and give it a pile of gameplay-module dependencies it has no other reason to have | Putting it in `CaseGame.Core` anyway (was the original placeholder plan from Report 014) — technically works but conflates "generic app lifecycle" with "this one scene's assembly," and forces Core to depend on Grid/Buildings/Units/Placement/Selection for no benefit to Core itself |
| 46 | Found and fixed two pre-existing data bugs while auditing the definition assets this feature touches, rather than leaving them (Report 017): (1) `BuildingDef_Barracks`/`BuildingDef_PowerPlant` had an empty `entityName` (a stale `buildingName` field from before Report 010's rename was still populated instead); (2) `UnitDef_Soldier3` had `maxHealth: 1`/`attackDamage: 1` instead of the brief's explicit 10 HP / 2 damage (requirement 9) | These are unambiguous correctness bugs, not judgment calls: (1) would show a blank building name on two UI panels this very feature just wired up, and (2) directly contradicts a numbered brief requirement. Both are asset-data-only fixes (no code/logic change), low-risk and easily reversible if the human disagrees with the correction — but silently leaving a known, explicit-requirement violation in place because it predates this feature would have been the wrong call | Leaving them for the human to notice separately — the brief's own evaluation criteria explicitly call out "edge cases will be examined" and correctness against the numbered requirements; finding this during an explicitly-requested integration audit and not acting on it would defeat the purpose of doing the audit |
| 47 | `PlacementController`/`SelectionController`'s `Update()` now both check `EventSystem.current.IsPointerOverGameObject()` and skip world-input handling when true (Report 017) | Both read `Mouse.current` directly every frame with no UI awareness — before this feature, they never coexisted with clickable UI in the same live scene, so the conflict was latent, not yet manifest. Now that the Production Menu and Info Panel are both interactive *and* live in the same scene as these controllers, clicking a UI row would otherwise also fire a world-space commit/cancel/select/deselect at whatever happened to render underneath that screen position — standard Unity idiom, applied where it was newly needed | Leaving it unguarded — would produce a real, reproducible interaction bug (clicking "produce" also deselecting/cancelling) the very first time someone actually plays the assembled scene |
| 48 | Camera repositioned to the grid's center and resized (orthographic size 10.5) via a formula derived from `GridDef_Default.asset`'s dimensions and the Production Menu/Info Panel's known fixed UI width (360 units each, 1920 reference width), rather than left at Unity's untouched default (0,0,-10, size 5) (Report 017) | The default camera was never pointed at the grid at all (grid spans world X:[0,20] Y:[0,12]; default camera framing shows mostly empty space below-left of it) — this needed *some* fix for the scene to be usable. The formula targets fitting the grid's width inside the screen strip the two side panels don't occlude, with modest padding | Leaving the camera untouched and asking the human to fix it by hand — the math is straightforward enough to compute directly; flagged as *unverified* (agent cannot enter Play Mode) rather than silently presented as correct, so the human knows to actually look at it |
| 49 | Split the draw-call/batching work (requirement 13) into two passes — architecture now (this report), numeric verification later, after visual polish — rather than doing both together | Human-directed, and matches how the project already treats visually-unverifiable work: batching *architecture* (shared materials, a sprite atlas, instancing flags) is cheap to set up early and expensive to retrofit once more assets exist, but the actual draw-call *count* is only meaningful measured against final content — measuring now would mean re-measuring again after every polish pass anyway | Doing a single combined pass at the very end — risks discovering a budget overrun with no time left to redesign; doing full numeric verification now — the numbers would be against placeholder-adjacent content and need re-confirming regardless |
| 50 | `SpriteAtlas_Gameplay.spriteatlas` packs entire folders (`Blue Buildings`/`Blue Units`), not just the individual sprites the 5 shipped definitions currently reference (Report 018) | The human is still doing visual polish (this project already imported full Animator Controllers/animation clips for these unit types, suggesting animated sprites may follow) — folder-level packing auto-includes new/changed sprites the human adds later with zero further scripting, at the cost of a modestly larger atlas (unused frames from the source spritesheets also get packed) | Hand-picking only the 5 currently-referenced sprites — smaller atlas today, but would silently stop covering new art the moment visual polish adds or swaps a sprite, quietly reopening the exact gap this report closes |
| 51 | New module named `CaseGame.CameraControl`, not `CaseGame.Camera` (Report 019) | A namespace named exactly `Camera` sharing scope with code that also needs `UnityEngine.Camera` as a field type is a real C# gotcha — the compiler can resolve the bare identifier `Camera` to the enclosing namespace instead of the type (`CS0118`), forcing `UnityEngine.Camera` to be fully qualified everywhere in the file. Renaming the namespace avoids the problem entirely rather than fully-qualifying every reference | Naming it `CaseGame.Camera` and always writing `UnityEngine.Camera` explicitly — works, but is a papercut on every future line in the module for no benefit |
| 52 | Building placement centers the footprint on the hover cell (`PlacementController.ComputeFootprintOrigin`), and `BuildingBase` self-manages its own grid footprint via `SetPlacement`/a new `GameEntityBase.OnEntityDied()` virtual hook, rather than (a) leaving corner-anchoring alone or (b) having `PlacementController` externally track "which cells does each placed building occupy" (Report 020) | (a) Corner-anchoring visibly misaligned centered building art from its actual footprint — a real visual bug once real (Tiny Swords) art replaced placeholders. (b) An external registry in `PlacementController` would need to survive independently of any single placement session (buildings persist long after being placed; `PlacementController` only tracks the *current* ghost) — colocating the footprint-origin state on the `BuildingBase` instance itself, released via a virtual method dispatched to *this* instance's current state, avoids the event-subscription-outliving-pooled-reuse hazard a persistent external subscription would have had (a stale subscription firing for a since-reused instance, unoccupying the wrong cells) | An external "placed buildings" registry Placement or Gameplay owns — works, but needs its own add/remove lifecycle bookkeeping that colocating the state on `BuildingBase` avoids entirely; a raw `+=` subscription to a per-instance death event from an external owner — the exact reuse-hazard pattern already avoided elsewhere in this project |
| 53 | "Remove Building" (`InfoPanelController.RequestRemoveBuilding`) is implemented as `building.ApplyDamage(building.MaxHealth)`, not a dedicated removal method/event (Report 020) | `ApplyDamage` reaching 0 already triggers the complete, correct death pipeline (pool-release via the existing `_onDied` callback, and — as of decision #52 — grid-footprint release via `OnEntityDied`). "Removed by the player" and "destroyed in combat" are the same end state (gone, cells freed, pool-released); building a second removal path would duplicate that pipeline for no behavioral difference. `SelectionController.SetSelectedBuilding` also gained a defensive `IsDead` staleness check (mirrors decision #39's soldier-pruning) since a selected building can now become invalid via a second, more immediate, player-triggered path, not just distant combat | A `RemoveBuildingRequestedEventChannel` carrying the building to a listener that manually calls `ReleaseFromGrid`+`Factory.Release` — this was the original design, dropped once it became clear `ApplyDamage` already does both automatically via the existing death pipeline, with no new event/module-wiring needed at all |
| 54 | Unit spawn-cell occupancy checks a *live* `UnitFactory.ActiveUnits` set (populated/pruned on create/death), not a persisted per-cell occupancy grid the way buildings use (Report 020) | Units move continuously; a persisted occupancy grid would need updating every time a unit moves (hooking into `SoldierBase`/`FollowPath`), which is real, unscoped complexity the brief never asks for (routing "around buildings," GI-7/8, not around other units). A live point-in-time scan at the one moment it's actually needed (spawn time) gets the requested behavior — "can't spawn on top of an existing unit" — without implying general unit-vs-unit collision or pathfinding-around-units | A `GridModel`-integrated unit-occupancy layer, continuously updated as units move — would satisfy this request too, but implies (and half-builds toward) unit-vs-unit collision/pathfinding the brief doesn't require and wasn't asked for here |
| 55 | Reversed decision #53: "Remove Building" now raises a dedicated `BuildingRemovalRequestedEventChannel` (`InfoPanelController` raises it; `PlacementController` subscribes to actually free the footprint/pool-release; `SelectionController` subscribes to clear a stale selection) instead of calling `ApplyDamage(MaxHealth)` — manual removal no longer touches Health/`_onDied` at all. `BuildingBase.OnEntityDied()` (combat-only now) and the new public `ReleaseFootprint()` (called directly by removal) both do the same footprint cleanup, reached by two fully independent triggers. `BuildingFactory.Release` also dropped its `prefab` parameter — it now looks up an instance's own pool internally (recorded at `Create` time), since manual removal doesn't have a "current prefab" the way an in-progress ghost placement does | Human-directed correction: Buildings must be `Creation -> Event, Removal -> Event, Death -> Handled by health` (Units: `Creation -> Event, Death -> Handled by health`) — three conceptually distinct triggers, not two, and Removal folding into Death was exactly the coupling this taxonomy rules out. This is decision #53's own originally-considered-and-dropped "alternatives rejected" design, reinstated. Closing the loop also required `SelectionController` to react to removal proactively (`HandleBuildingRemoved`) since removal no longer sets `IsDead`, so the old lazy staleness check alone stopped covering that path | Keeping #53's `ApplyDamage`-reuse (simpler, but couples an explicit player action to the combat-death pipeline — exactly what was ruled out); routing Removal through a new `Health`-adjacent API instead of a fully separate event — would still entangle the two triggers this taxonomy keeps apart |
| 56 | Selection feedback (`GameEntityBase.SetSelected`) switched from tinting `spriteRenderer.color` yellow to toggling a dedicated `outlineRenderer` child (`SpriteSelectionOutline.shader`/`M_SpriteSelectionOutline.mat`, Report 022) — a hand-authored HLSL shader that draws a solid ring only where the sprite's own alpha is transparent but a neighboring texel (offset by a data-driven `_OutlineThickness`, in texels) is opaque, so it never overlaps the sprite's own silhouette. The reset-on-pooled-reuse concern from decision #38 still applies, just relocated: `Initialize` now resets `outlineRenderer.enabled = false` instead of `spriteRenderer.color = white` | Same reasoning as decision #28 (the Placement ghost's desaturate-then-tint shader): multiplying a full-color sprite by a tint color blends with whatever hues are already in the art instead of reading as a clean, unambiguous signal — a real problem once real (Tiny Swords) art replaced placeholders. A real outline reads clearly regardless of the underlying art's colors. Outline thickness kept small (default 1.5 texels) relative to `SpriteAtlas_Gameplay`'s 4px packing padding (decisions log #50) to avoid the neighbor-sampling technique leaking into an adjacent packed sprite | A sprite-mask-based outline (Unity's built-in `SpriteMask` component) — doesn't produce an outline *ring* on its own, would need combining with a scaled-up silhouette anyway; scaling up a duplicate sprite behind the real one (simpler, but the produced "outline" thickness isn't uniform around a non-convex/non-uniform silhouette the way per-pixel neighbor-sampling is) |
| 57 | `SoldierBase.FollowPath` now advances each path step over a fixed `StepDuration(moveSpeed) = 1f / moveSpeed` seconds (via a time-based `InterpolateStep` Lerp), instead of `Vector3.MoveTowards(..., MoveSpeed * Time.deltaTime)` — a constant *world-distance* speed. Both are pure static methods on `SoldierBase` (Report 023) | Human-directed correction: `MoveSpeed` should mean "N grid cells per second," with a diagonal step counting as 1 cell like an orthogonal one, not √2 world-distance units — under the old `MoveTowards` approach, `GridModel.CellCenterToWorld`'s diagonal step covers `cellSize·√2` world distance vs. an orthogonal step's `cellSize`, so at the same nominal `MoveSpeed` a diagonal step took ~41% longer, contradicting "shortest path" (GI-7/8) being measured in *steps*. Extracting `StepDuration`/`InterpolateStep` as pure functions (rather than leaving the math inline in the coroutine) makes the actual timing contract — duration depends only on `moveSpeed`, never on a step's real distance — directly unit-testable, since the coroutine itself can't run under this project's batchmode EditMode test runner (`ENVIRONMENT.md`'s Awake/Update-doesn't-fire gotcha extends to coroutines needing a live Update loop). Note: since `GridDef_Default.asset`'s `cellSize` is 0.5 (not 1), existing `UnitDefinition.moveSpeed` values (3/5/3) now mean a real ~2× slower felt speed than before (previously ~6/10/6 cells/sec effectively, now literally 3/5/3) — a data-only retune, not a code change, left for the human to adjust by feel if desired | Keeping `MoveTowards`/world-distance speed (matches decision #24's diagonal-cost-√2 A* heuristic, but that heuristic is about *pathfinding cost comparison*, not real-time movement speed — conflating the two was the bug); scaling `MoveSpeed` per-step by the step's actual distance to compensate (would still call diagonal steps "faster" in cells/sec than the stated rate, just hidden inside the math instead of visible as a duration difference) |
| 58 | `ProducibleUnitsContainer` (`InformationPanel.prefab`) uses a built-in `GridLayoutGroup` (Fixed Column Count) + `ContentSizeFitter` (vertical Preferred Size) instead of a custom C# grid-layout system, and instead of leaving the pre-existing `HorizontalLayoutGroup` in place (Report 024) | Human explicitly asked for a grid view (N columns, wraps into rows, partial last row) with shape/size adjustable via the editor. `GridLayoutGroup`'s own Inspector already exposes exactly this (Cell Size, Spacing, Constraint, Constraint Count) with zero code — writing a bespoke layout calculator would duplicate a solved problem for no benefit (golden rule 2). `ContentSizeFitter` added alongside it so the container's height isn't a guessed fixed pixel value that clips a 3rd row once more producible-unit entries exist than fit in one row | A custom `ProducibleUnitsGridLayout` MonoBehaviour computing row/column positions itself — unnecessary reinvention of `GridLayoutGroup`; leaving a fixed pixel height on the container instead of `ContentSizeFitter` — would need the human to guess a "big enough" height that still silently clips if producible-unit count ever grows past whatever was guessed |
| 59 | `PanelContent` (`InformationPanel.prefab`) gained a `VerticalLayoutGroup` (Child Control Width on, Child Control Height off, Force Expand Width **off**) stacking its 4 children; `BuildingIcon`/`RemoveButton` keep a `LayoutElement` with a fixed `preferredWidth` (128/160), while `BuildingName`/`ProducibleUnitsContainer` each got a `LayoutElement` with `flexibleWidth = 1` so only *they* stretch to fill the panel's width (Report 025, corrected same-day — the initial version had Force Expand Width **on**, which the human caught: per `HorizontalOrVerticalLayoutGroup`'s own source, `childForceExpand` forces *every* child's flexible size to at least 1 regardless of its own `LayoutElement`, stretching `BuildingIcon`/`RemoveButton` to the full 328 panel width despite their `preferredWidth`) | `RemoveButton` was anchored to a fixed position independent of `ProducibleUnitsContainer`'s actual (now content-driven, decisions log #58) height — correct only when the grid happened to be exactly 1 row tall, the same class of bug Report 024 just fixed for the grid's own overflow. `Child Control Height = false` lets the parent still read each child's real height (including the grid's own `ContentSizeFitter`-computed height) for positioning the next child, without forcing a height on any of them. Force Expand Width is all-or-nothing per group (no per-child opt-out), so achieving "some children stretch, some don't" requires turning it off entirely and driving the stretch/don't-stretch split per-child via each one's own `LayoutElement.flexibleWidth` instead | Leaving `RemoveButton` at its old fixed anchored position — would silently overlap the grid again the moment producible-unit count exceeds 1 row, reintroducing materially the same bug from a different angle; a second `ContentSizeFitter` on `PanelContent` itself to auto-shrink the whole panel — not asked for, and out of scope (the panel's overall size staying fixed while its *contents* reflow is the actual request); relying on `LayoutElement.preferredWidth` alone with Force Expand Width on — doesn't work, since force-expand overrides every child's flexible size unconditionally |
| 60 | `InfoPanelController.SetSelectedBuilding` now calls `LayoutRebuilder.ForceRebuildLayoutImmediate` on `panelRoot` after spawning producible-unit icons, and `DestroyView` now always uses `DestroyImmediate` (was `Destroy` in Play Mode, `DestroyImmediate` only in Edit Mode/tests) (Report 025, same-day bugfix) | Human found: selecting a building showed the *previous* selection's stale panel layout for one frame, self-correcting only on the *next* selection change. Root cause was two-layered — (1) Unity's `GridLayoutGroup`/`ContentSizeFitter`/`VerticalLayoutGroup` rebuild lazily/deferred by default, so freshly-instantiated icons don't resize the panel until a later automatic pass; forcing it immediately fixes growing (fewer→more producible units). (2) Shrinking (more→fewer, e.g. Barracks' 8 to Power Plant's 0) still failed even with the forced rebuild, because `Destroy()` in Play Mode defers actual removal to end-of-frame — the forced rebuild still measured the about-to-be-destroyed old icons as real children. `DestroyImmediate` removes them from the hierarchy synchronously, so the same-frame forced rebuild sees the correct, already-updated child list either way | Only forcing the rebuild without also switching to `DestroyImmediate` — fixes the growing case but leaves the shrinking case broken, exactly what the human's own test matrix (4 direction/deselect combinations) caught; keeping `Destroy()` in Play Mode and instead reparenting cleared icons out of `producibleUnitsContainer` before destroying them — achieves the same immediate exclusion from layout but adds a "graveyard" transform and a UI-reparenting corner case (Canvas/RectTransform detach) for no benefit over just using `DestroyImmediate`, which is safe here since `ClearProducibleUnitIcons` only ever iterates its own local list, never Unity's live Transform children collection |
| 61 | UI visual polish (Report 026) done as: (a) `ProductionMenu.prefab`'s root anchor trimmed (`AnchorMax.y` 1 → 0.94) to free a top strip for a `ProductionMenuBanner` sibling in `Gameplay.unity`'s Canvas, rather than touching the `ScrollRect`'s own `Viewport`/`Content` anchors directly; (b) the Information Panel's banner added inside `PanelContent`'s existing `VerticalLayoutGroup` stack (shown/hidden with the rest of the panel) rather than as an always-visible sibling of `panelRoot`; (c) the Main Menu's "How to Play" screen built by duplicating the Settings button/panel (`Object.Instantiate`) and stripping Settings-specific children + the cloned `SettingsController` component, rather than hand-authoring new TMP/Button hierarchies from scratch | (a) `ProductionMenu`'s `Viewport` RectTransform has unusual-looking anchor values (`AnchorMin`/`AnchorMax` both `(0,0)`, `sizeDelta (0,0)`) that the agent didn't fully trace the runtime behavior of — shrinking the *parent* instead lets every child (Viewport, both Scrollbars) resize proportionally via Unity's own anchor math with zero risk of touching whatever makes the ScrollRect actually work today. (b) `panelRoot` (`PanelContent`) is the single GameObject `InfoPanelController.SetSelectedBuilding` toggles active/inactive for the entire panel — making the banner always-visible would need a structural split (a persistent sibling header + a toggled content pane) that wasn't asked for; reusing the stack that Report 025 already built for exactly this "add a child, it flows correctly" purpose is the lowest-risk fit. (c) `SettingsController.OnEnable` unconditionally dereferences `fullscreenToggle`/`resolutionDropdown`/`applyButton` — cloning `SettingsPanel` without removing this component would `NullReferenceException` the instant `HowToPlayPanel` is shown, since those children are exactly the ones this feature needs to remove | (a) Adjusting `Viewport`'s own anchors/`sizeDelta` directly to reserve the same space — more "correct-looking" in isolation, but risks silently breaking the scroll view given the agent's uncertainty about why its current values look the way they do; (b) restructuring `InformationPanel.prefab` to separate "always-visible chrome" from "toggled content" — a bigger, unrequested change for a `-not asked for-` always-on requirement; (c) hand-building the How-to-Play panel's TMP/Button hierarchy from `new GameObject`+`AddComponent` calls — more code, more surface area for a subtle Button/Image/TMP wiring mistake the agent can't compile-check away, when duplicating a known-working panel and trimming it is strictly safer |
| 62 | Ranged combat & the sustained-attack overhaul (Report 027) implemented as: (a) range/approach checks use the target's own `transform.position` (a single reference cell via `GridModel.WorldToCell`), not footprint-aware distance to a building's occupied cells; (b) `MoveTo`/`Attack` share one `_actionCoroutine` slot on `SoldierBase` rather than two independently-tracked coroutines; (c) `SelectionController.HandleRightClick`'s target parameter changed from `IDamageable` to `GameEntityBase`; (d) `Projectile` is a plain pooled MonoBehaviour, not a `ParticleSystem` | (a) `Units` cannot reference `BuildingBase` — `Buildings` already depends on `Units` (for `ProducibleUnits`), so the reverse would be circular, the exact reasoning decision #43 already established for spawn positions. Large-footprint buildings may need a generously-sized `attackRange` set by whoever configures the unit; refining this to true nearest-footprint-edge distance is a clean, isolated follow-up if it ever looks wrong, not a blocker now. (b) A unit can only ever be doing one of {walking to a point, walking-into-range-then-attacking} at a time — the human's own cancellation rule (a move order cancels an attack, and vice versa) is exactly "only one action slot," so two independent coroutine fields would just need to manually cancel each other anyway, with no benefit. (c) `IDamageable` alone has no position/transform, but both range-checking (this feature) and projectile-tracking need the target's live position — `GameEntityBase` (which every real attack target already is at the call site, per `SelectionController.HitTestEntity`) provides both `ApplyDamage` and `.transform` without widening the general-purpose `IDamageable` interface itself. (d) The requirement is precise per-target position tracking plus an exact "arrived → apply damage" trigger; reproducing that with `ParticleSystem` would need manually driving individual particles via `SetParticles`/`GetParticles` every frame anyway, no ergonomic win over a MonoBehaviour, while losing straightforward `Update()`-based homing and clean arrival timing — `ParticleSystem` remains the right tool for a *separate*, later concern (impact bursts, muzzle flash, backlog item 19), not the projectile's own flight | (a) A `Buildings`-aware footprint-distance helper living in a shared/lower module (e.g. `Grid`) both `Units` and `Buildings` could reference — a real, cleaner option, but scoped out for this pass since it wasn't asked for and the simpler per-position check already satisfies every explicit requirement; (b) two independent coroutine fields with manual cross-cancellation — strictly more state to keep in sync for no behavioral difference given they're already mutually exclusive by design; (c) extending `IDamageable` itself with a position property — would widen a deliberately minimal, `Combat`-module-owned interface (decision reasoning mirrors `Health`'s own "solves a problem that doesn't exist here" note, decision #19) just to serve two `Units`-only callers; (d) a `ParticleSystem` with velocity-over-lifetime/attractor modules aimed at a moving target — imprecise, hard to guarantee exact arrival-timing for damage application |
| 63 | Combat/UI bugfix pass (Report 028), 3 human-flagged issues: (a) `GameEntityBase` gained a virtual `GetNearestOccupiedCell(GridModel, Vector2Int)` (default: own cell), overridden in `BuildingBase` to clamp the attacker's cell into its footprint rectangle — `SoldierBase.AttackRoutine` now calls this instead of `grid.WorldToCell(target.transform.position)` for both the range check and `FindApproachCell`'s target argument; (b) `Projectile` gained `FacingRotation` (pure) plus an inspector-tunable `spriteForwardOffsetDegrees`, applied to `transform.rotation` every `Update` so the arrow visibly points along its travel direction; (c) `m_PreserveAspect: 1` set directly on the 3 icon `Image` components that were stretching fixed-size sprites (`InformationPanel.prefab`'s `BuildingIcon`, `ProducibleUnitIcon.prefab`'s `Image`, `ProductionMenuItem.prefab`'s `Icon`) | (a) This is exactly the "clean, isolated follow-up" decision #62(a) flagged and deliberately deferred — the human found the un-clamped version looked wrong in practice once a multi-cell building was actually attacked from its side. Putting the method on `GameEntityBase` (not a new `Buildings`-only helper `Units` would need to reference) keeps the Units→Buildings circular-dependency boundary from decision #62(a)/#43 intact: units still only ever call a method on the `GameEntityBase` they already hold, `BuildingBase`'s override is invisible to `Units`. Units keep the trivial default (footprint is always 1×1) with zero extra code. (b) The Arrow.png art's own default-drawn facing direction wasn't verified against the code (no prior rotation logic existed to check it against), so a hardcoded angle risked being backwards or off by 90°; making the offset a serialized field lets the human correct it by eye in the Inspector without a code change, matching this project's established "test by eye" tolerance for visual-only tuning. (c) `Image.preserveAspect` is a built-in Unity UI behavior (letterbox within the existing fixed-size `RectTransform`, no layout/anchor changes) — exactly matches "stop stretching," with no code or size changes needed | (a) A `Buildings`-aware footprint-distance helper in a shared lower module (`Grid`) — the more general fix decision #62 already named as the rejected alternative; still not pursued since the `GameEntityBase` virtual-method shape solves it with less surface area and no new module dependency; (b) hardcoding the offset from a visual guess — riskier than exposing it, and this project already treats sprite-facing/orientation as inspector-tunable elsewhere (e.g. `SpriteRenderer.flipX` usage); (c) resizing the icon `RectTransform`s to each sprite's native aspect ratio instead — would require per-building/per-unit custom sizing and fights the whole point of a uniform icon grid/row, whereas `preserveAspect` letterboxes correctly inside a uniform box automatically |
| 64 | Grid line rendering (Report 029, backlog item 16) implemented as: (a) a single combined mesh (`GridLineMeshBuilder`, one quad per line, one draw call for the whole board) instead of one `LineRenderer` per line; (b) `lineColor`/`lineThickness` moved onto `GridDefinition` (data), while `lineMaterial`/`sortingOrder`/the visibility toggle stay on `GridView` (view/rendering config); (c) live Edit-Mode updates via `[ExecuteAlways]` + a cheap `Update()` signature comparison, not relying on `Awake`/`OnValidate` alone; (d) the toggle is a plain `SetLinesVisible(bool)` method, no event channel; (e) no scene UI button wired this pass | (a) A real-sized board (the default `GridDef_Default.asset` is 20×12 → 34 lines) drawn as 34 separate `LineRenderer`s would cost up to 34 extra SetPass calls, trivially blowing GI-12's <20 budget the project has been deliberately protecting since decisions #18/#49 — a single mesh is exactly one draw call regardless of board size. (b) Mirrors the existing Model/View split this project already uses everywhere (`GameEntityDefinition` vs. `GameEntityBase`, decision #22): color/thickness are grid *data* a designer tunes per `GridDefinition` asset (the backlog explicitly named these as belonging there), while which material/shader renders it and where it sorts are concerns of *this specific renderer instance*, not the abstract grid definition. (c) `GridView.Awake()`/`OnValidate()` alone can't catch edits made to the *separate* `GridDefinition` ScriptableObject asset — editing that asset's Inspector fires its own `OnValidate`, not `GridView`'s, and there's no existing event link between an SO asset and every scene object that happens to reference it; polling a cheap cached-value comparison once per Edit-Mode `Update` catches every case (self-edits, `GridDefinition`-asset edits, swapping to a different `GridDefinition` entirely) with no new event-wiring machinery, and costs nothing during Play Mode (guarded by `!Application.isPlaying`). (d) Nothing else in the project needs to react to "grid lines toggled" — it's local render state on one instance, so a direct method call (matching how `InfoPanelController.RequestRemoveBuilding` is wired straight to a Button's `OnClick`) is simpler than an SO event channel with a single always-known subscriber. (e) The backlog text itself called the UI button "optional... human's call at implementation time" — shipping the toggle API now and leaving the button as a deliberately-scoped-out follow-up avoids guessing where the human wants it to live (Settings screen? an in-scene HUD toggle?) | (a) One `LineRenderer` per grid line — far simpler code, but directly reintroduces the draw-call problem this project has structured its entire visual-polish approach around avoiding; a `GL.Lines`/`Graphics.DrawMeshNow` immediate-mode approach via `Camera.onPostRender` — works in Play Mode but doesn't respect Unity's per-object sorting layer/order system the "renders behind everything else" requirement needs, and doesn't preview in Edit Mode without extra plumbing. (b) Keeping `lineColor` on `GridView` alone (as it was pre-Report-029) — works but drifts from the project's own established Model=SO/View=MonoBehaviour split and from the backlog's explicit instruction. (c) A `GridDefinition`-side C# event raised from its `OnValidate`, subscribed to by every live `GridView` — more moving parts (subscribe/unsubscribe lifecycle across domain reloads) for the same practical result the simpler polling approach already gives for free. (d) A `GridVisibilityRequestedEventChannel` — unnecessary decoupling machinery for a call with exactly one implementation and no cross-system listeners today. (e) Guessing a location and wiring a button anyway — risks producing UI the human didn't ask for in the wrong place, exactly what golden rule 2 (one feature at a time, no speculative additions) warns against |
| 65 | Environment/terrain visuals (Report 030, backlog item 17) scoped to purely decorative (human-confirmed, no occupancy changes), implemented as: (a) a flat solid-color "Water Background color" `Tile` painted as a `Tilemap` frame around the grid's bounds (margin 40 cells / 20 world units), leaving the grid's own interior cells unpainted so the existing camera background shows through as "land"; (b) a ring of static `Rock1–4.png` decorations (not the pack's animated Bush sprites) placed just outside the grid edge via a new pure `BorderDecorationLayout` (`CaseGame.Environment`); (c) both baked once into `Gameplay.unity` by a throwaway editor script — no new runtime MonoBehaviour, the terrain doesn't react to `GridDefinition` changes the way `GridView`'s grid lines do (decisions log #64); (d) `Tileset`/`Decorations/Rocks` added as new `SpriteAtlas_Gameplay` packable folders | (a) The actual Tiny Swords `Tilemap_colorN.png` sheets are autotile "floating island" blob pieces (grass-topped cliffs) whose 45 tile pieces only read correctly when assembled via specific adjacency rules — visually verifying a hand-assembled or scripted autotile composition isn't possible from this environment (no Editor/Play Mode screenshot), so committing to it risked shipping a visibly broken result with no way to catch it before the human's own review. The pack's one genuinely safe-to-repeat asset is the flat solid-color water background (zero seam risk, uniform color) — using it as a frame around the grid, with the interior deliberately left as the existing background, sidesteps the entire autotile-correctness problem while still using real pack art and satisfying "the ground isn't empty" for the area around the board. (b) Same reasoning extended to decorations: `Rock1–4.png` are plain static single sprites (safe to place as-is); the pack's Bush sprites are 8-frame animation strips (`Bushe1_0..7`) — picking a "resting" frame blind, with no way to preview it, risked an arbitrary-looking mid-animation pose. (c) This is one-time level dressing requested as visual polish, not a data-driven system another feature needs to query or recompute — matches the same "static, not runtime-bound" scoping already applied to Report 029's water-adjacent visuals conceptually, but taken further here since even Edit-Mode live-preview wasn't attempted (no ambiguity to resolve at edit time — the layout only needs computing once). (d) Directly matches the backlog's explicit instruction that terrain art needs its own packable atlas entry, mirroring the existing two folder-level building/unit packables (decisions log #18) | (a) Hand-assembling the cliff/grass autotile sheet into a proper island shape — the more thematically ambitious option, explicitly rejected due to zero visual-verification capability in this environment; a `RuleTile`-based auto-tiling setup (`com.unity.2d.tilemap.extras`) — would reduce the manual-assembly risk but requires a new package (CLAUDE.md golden rule 6 approval) and still needs human visual QA before trusting the result, out of scope for a first decorative pass. (b) Including animated Bush props with a guessed static frame — cosmetically riskier for no clear benefit over the already-sufficient rock ring. (c) A runtime `MonoBehaviour` that regenerates the border ring whenever `GridDefinition` changes (mirroring `GridView`'s Report 029 live-preview) — real over-engineering for content that, by design, isn't meant to visually react to gameplay-grid resizing in this pass; if the grid's dimensions become designer-tunable in practice, regenerating this decoration is a cheap, isolated follow-up (rerun the same throwaway-script pattern), not a reason to add standing runtime machinery now |
| 66 | Camera pan/zoom clamping (Report 031, human-requested: "never show the background color/skybox") implemented as: (a) a new `GridDefinition.TerrainMargin` field — the single source of truth for how far the water backdrop extends beyond the grid, read by *both* the baked terrain repaint and `CameraController`'s new bounds; (b) `CameraController.SetBounds(min, max)` + a pure/testable static `ClampToBounds`/`ClampAxis`, applied after every `Pan`/`Zoom`, with an axis whose viewport is wider than the bounds centering instead of clamping; (c) `GameplayBootstrap` computes the bounds from `GridModel.CellToWorld` ± `TerrainMargin` and calls `SetBounds` once in `Start()` — the first time `CameraController` needs any wiring from the composition root at all; (d) `TerrainMargin` bumped from Report 030's hardcoded 20 world units to 30 ("extend the water a bit") | (a) Before this report, the water frame's extent (Report 030) was a constant local to a since-deleted throwaway script — reading it from anywhere else (the camera bounds) would have meant duplicating that number in two disconnected places with no compiler-enforced link between them, exactly the kind of magic-number drift this project's "everything data-driven" philosophy (decision #5) exists to prevent. Promoting it to `GridDefinition` (which already carries `LineColor`/`LineThickness` as of decision #64 — visual tuning values tied to the grid) keeps one designer-editable number driving both the paint extent and the clamp. (b) Clamping the *camera center*, not the raw pan delta, means it's correct regardless of how the position got there (a single huge drag, or zooming out while already near an edge) — computing it from `orthographicSize`/`aspect` directly (rather than hardcoding a viewport size) means it stays correct at any zoom level or aspect ratio automatically, including on window resize. The "center if the viewport is too wide for the bounds" branch is a deliberate edge-case decision: if `TerrainMargin` is ever set very small (or the board very large relative to `maxOrthographicSize`), the alternative — clamping anyway — could push the camera to a physically nonsensical split position; centering the camera on the too-small axis is the visually correct fallback (some background may show at extreme zoom in that narrow case, but the camera itself doesn't glitch). (c) Mirrors exactly how every other controller here gets its runtime dependencies — from `GameplayBootstrap.Start()`, not `Awake`, so ordering against `GridView`'s own `Awake`-built `GridModel` is never a race (same reasoning as decision #45). (d) A modest, purely aesthetic bump — more room to pan and look around before hitting the water's edge | (a) Hardcoding the same margin number again at the camera-bounds call site — the two values could silently drift apart the next time either one changes, exactly the bug class decision #5 already rules out elsewhere; a bounds value baked directly into the scene (matching Report 030's original "static, not runtime-bound" reasoning for the terrain itself) — rejected here specifically because the *camera*, unlike the terrain's own geometry, has no equivalent "this never needs to change without a full re-author" justification, and a live `GridModel`-derived computation costs nothing extra. (b) Clamping the raw pan/zoom delta before applying it (reject the input rather than correct the resulting position) — more fiddly to reason about for the zoom case (which axis becomes too wide depends on the *result*, not the delta itself) for no benefit over simply re-clamping the final position every time. (c) Giving `CameraController` its own `Initialize` signature independent of `GameplayBootstrap`'s existing `Start()` pattern (e.g. a `[SerializeField]` baked bounds pair set once in the Editor) — would drift out of sync with the grid the moment `GridDefinition`/`TerrainMargin` changes, the exact problem (a) already solves by computing it live |
| 67 | Procedural island tilemap generation (Report 032, human-requested follow-up to Report 031's hand-painted reference) implemented as: (a) a new `IslandTileSet` SO holding 12 named `TileBase` references (9 for the grass 9-slice — 4 corners, 4 edges, center — 3 for the cliff row: left/middle/right); (b) a pure/testable `IslandTilemapLayout` computing grass placements for any `columns`×`rows` (corners/edges/center by position) and cliff placements for one row at `y = -1` spanning `columns` width; (c) `IslandTerrainView`, an `[ExecuteAlways]` view rebuilding both Tilemaps from `GridDefinition.Columns`/`Rows` on `Awake`/`OnValidate`/an Edit-Mode `Update` signature check — the exact same live-preview pattern `GridView` already established (decisions log #64); (d) the 12 tile references were populated automatically from the human's own hand-painted `Island_Grass`/`Island_Cliff` Tilemaps via a throwaway script that read each reference cell (corners, edge midpoints, center, cliff ends/middle) directly off the live `Tilemap` objects with `Tilemap.GetTile`, then — critically — verified the extracted set reproduces every single originally-painted cell exactly (plus a `cellBounds` check ruling out any painted cells outside the expected rectangle/row) *before* creating the asset or wiring anything live, so a bad extraction could never silently overwrite the human's manual work | (a)/(b) This mirrors `GridLineMeshBuilder`'s own shape from Report 029 almost exactly: pure geometry class + a small SO for the tunable inputs + a thin `[ExecuteAlways]` view — reusing an already-proven pattern rather than inventing a new one for what is structurally the same kind of problem ("regenerate a rendered artifact from grid dimensions"). Landing the palette on a dedicated `IslandTileSet` SO (rather than more fields directly on `GridDefinition`, which already carries the grid-line visual fields from decision #64) keeps `GridDefinition` scoped to the *grid's own* data, since `TileBase` references are Environment-module concerns a designer might swap independently of the grid's layout (e.g. a different art style/season reskin later) — matches the same Model/View module-boundary reasoning as decision #66(a). (c) Copying `GridView`'s exact rebuild-triggers (not inventing new ones) means the same known-working "editing `GridDefinition` previews live in Edit Mode without Play Mode" guarantee extends to the island for free. (d) Reading the tile references directly from the human's own painted work removes an entire class of error (me guessing/mistyping which of the 45 tileset pieces is "top-left corner" vs. "left edge" from a description) that Report 031 explicitly avoided taking on; the pre-commit verification pass exists because `Tilemap.ClearAllTiles()` is destructive and irreversible via this scripted path — verifying byte-for-byte equivalence *before* that call is the only way to guarantee the human's manual painting work is never at risk from an extraction mistake | (a) Adding the 12 tile fields directly onto `GridDefinition` instead of a new SO — simpler (no new asset to create/wire) but conflates "how big is the grid" with "what does the terrain look like," a distinction decision #66(a) already drew a line around for `TerrainMargin` specifically because it's shared across two systems; tile art has no such cross-system sharing need. (b) A `RuleTile`-based approach instead of explicit corner/edge/center logic — would need the `com.unity.2d.tilemap.extras` package (golden rule 6 approval) and doesn't reduce the actual complexity here (the 9-slice rule is already just 9 fixed cases, trivially expressed as plain conditionals). (d) Asking the human to manually re-identify and drag in 12 tile references themselves — redundant work given they'd already implicitly chosen every one of those exact pieces by hand-painting the reference island, and strictly more error-prone (typo'd/misidentified reference) than reading the ground truth directly off the Tilemap API; skipping the pre-commit verification and trusting the extraction — unacceptable risk of destroying manually-authored content with no undo path from a batchmode script |
| 68 | Unit animations (Report 033, backlog item 18) implemented as: (a) `SoldierBase` gained a plain `[SerializeField] Animator animator` (nullable — every call site null-checks, matching the existing `spriteRenderer`/`outlineRenderer` pattern on `GameEntityBase`) driving a bool `IsMoving` (toggled around `FollowPath`, also reset in `CancelAction`) and a trigger `Attack` (fired once per `PerformAttack`); (b) a throwaway script built the *entire* Idle/Run/Attack state machine (parameters + 5 transitions: Idle↔Run on the bool, Any State→Attack on the trigger, Attack→Run/Idle on exit time branching on the bool) on the Tiny Swords `Warrior_Blue`/`Archer_Blue`/`Pawn_Blue` Animator Controllers, which shipped as unwired scaffolds (every clip present as a bare state, `m_Transitions: []`/`m_AnimatorParameters: []` empty everywhere); (c) the `Animator` component was added to each soldier prefab's `Visuals` child specifically (not the prefab root), because the pack's clips bind their `SpriteRenderer.m_Sprite` curve via an *empty* GameObject path — resolved relative to whatever GameObject holds the `Animator` — and the real `SpriteRenderer` lives on `Visuals`, not the root; (d) Soldier 3 keeps its existing "Pawn" art/prefab (human-confirmed, not swapped to a combat archetype) — its `Attack` trigger maps to the Pawn controller's `Pawn_Interact Knife_Blue` state (a generic tool-use clip, not a sword swing), since Pawn has no combat animation and the human explicitly chose not to reskin Soldier 3 for this pass | (a) Mirrors this project's own established "humble direct reference, not an event layer" convention for rendering handles exactly — `Animator` is exactly as much a "rendering detail" as `spriteRenderer`, so treating it identically (not inventing a `SoldierAnimationController` indirection) keeps `SoldierBase` the single owner of both its state *and* how that state is reflected, with zero new coupling. Resetting `IsMoving` in `CancelAction` specifically exists because `StopCoroutine` abandons `FollowPath` wherever it's currently suspended with no cleanup — without this, an attack order interrupting a mid-stride move would leave the run animation stuck on forever, a real bug the human's own move-cancels-attack/attack-cancels-move rule (decisions log #62) would trigger constantly. (b) The pack's Animator Controllers are literally just labeled clip containers, not authored state machines — the backlog's own note ("this may be mostly wiring") anticipated exactly this, so building the graph via `UnityEditor.Animations.AnimatorController`'s scripting API (rather than by hand in the Animator window) is both correct and far more mechanically reliable across 3 near-identical controllers; verified afterward by reading each `.controller`'s serialized parameters/transition count back, matching this project's standing "always verify a throwaway script's structural result by reading the file, never trust a clean exit code alone" discipline. (c) Confirmed by reading one of the pack's own `.anim` files directly (`path:` was empty, binding `classID: 212` / `m_Sprite`) rather than assumed — putting the `Animator` on the prefab root would have silently done nothing, since an empty-path curve only resolves against the GameObject the `Animator` itself sits on, and re-authoring every clip's binding path instead would mean touching dozens of third-party `.anim` assets for a one-line placement fix. (d) Matches the human's own explicit answer to this exact trade-off (Pawn/Interact vs. swapping to Lancer with or without full directional facing) — recorded here as the decision actually taken, not re-litigated | (a) A separate `SoldierAnimationView`/controller class subscribing to `SoldierBase` state via an event — real indirection for a case with exactly one consumer and no cross-system decoupling need, the same class of rejected alternative as decisions #64(d)/#66(c). (b) Hand-wiring each of the 3 controllers' transitions in the Animator window — the exact "large/error-prone one-off wiring" case CLAUDE.md's editor script policy carves out the throwaway-script exception for, especially repeated 3× with only clip-name differences. (c) Re-authoring all affected `.anim` clips' binding paths to target `Visuals` explicitly instead of relying on the empty-path convention — far more invasive (dozens of third-party asset edits) for no behavioral difference over just placing the `Animator` where the empty path already resolves correctly. (d) Swapping Soldier 3 to Lancer (either fixed-direction or fully directional) — both explicitly declined by the human; fully directional was also flagged during the question itself as real added scope (a new facing-direction concept on `SoldierBase` mapped to ~8 directional variants) better suited to its own follow-up if ever wanted |
| 69 | Post-hand-test polish/bugfix pass (Report 034), 4 human-flagged items: (a) unit facing implemented as `GameEntityBase.SetFlippedHorizontally` (`SpriteRenderer.flipX` on both `spriteRenderer`/`outlineRenderer`) rather than flipping `transform.localScale.x` on the whole GameObject or a new visuals-grouping parent (both suggested by the human, with an explicit invitation to counter-argue) — driven by a pure `SoldierBase.FacesLeft(fromX, toX)`, called from `FollowPath` (movement direction) and `PerformAttack` (attack-target direction); (b) the Archer's `Projectile` launch is deferred from the attack tick to a new `SoldierBase.ReleaseAttack()`, called via a new `SoldierAnimationEvents` relay component (on the `Visuals` child) wired to an Animation Event on `Archer_Shoot_Blue.anim` at `time: 0.7` (its last frame) — melee's instant damage is unchanged; (c) `SelectionController` gained a `PlacementController` reference and now skips its own `Update()` entirely while `IsPlacing` is true; (d) `CameraController.Zoom` is now fed `NotchesFromRawScrollDelta(scroll)` instead of the raw `Mouse.scroll.ReadValue().y` | (a) `SpriteRenderer.flipX` is the idiomatic Unity 2D tool for exactly this (mirror a sprite without touching any Transform), and critically doesn't touch `Collider2D` shapes, child-transform math, or anything future code might read off `transform.localScale`/`lossyScale` — a negative root/parent scale would achieve the same *visual* result today (this project's colliders are simple, symmetric, non-Rigidbody shapes, so it would likely have been harmless), but `flipX` has zero blast radius by construction rather than "probably fine," and `GameEntityBase` already directly owns both renderer references, so no new parent GameObject or hierarchy change was needed either. Applying it to both `spriteRenderer` and `outlineRenderer` together keeps the selection outline from drifting out of sync with the sprite it traces — the same "keep both renderers in lockstep" concern already present in `Initialize`'s sprite/enabled-state resets. (b) Every actual hit/shot already funnels through the single `PerformAttack` call site (decisions log #62); deferring only the *ranged* branch's projectile launch — and only when an `Animator` is actually present, falling back to the old immediate-launch behavior otherwise — means melee, and any soldier without a wired `Animator`, are entirely unaffected. The relay component is necessary, not a style choice: Animation Events call `SendMessage` on the GameObject the firing `Animator` itself sits on (`Visuals`, decisions log #68(c)), which has no `SoldierBase` to call directly. Storing the pending target/damage/factory as plain fields (overwritten by whichever attack tick fires next, discarded if the target died before the event fires) mirrors the same "last one wins, dead-target guard" tolerance already established for `Projectile` itself (decisions log #39/#52/#55 lineage) rather than inventing a queue. Event placed at the clip's last frame (`time: 0.7` of 0.0–0.7 across 8 frames) as a best-effort release-pose guess — flagged as human-tunable, not verified visually from this environment. (c) `PlacementController`/`SelectionController` each independently read `Mouse.current.rightButton.wasPressedThisFrame` in their own `Update()` with no awareness of each other, so a single right-click was being consumed by *both* — canceling the ghost `and` issuing a move order to the selected soldier(s) at the same clicked cell. The identical gap exists for left-click (committing a placement could also deselect the current selection via `HandleLeftClick`'s default/empty-cell case) — fixed alongside the reported right-click case since it's the same root cause, not a separate bug. Skipping `SelectionController.Update()` entirely while placing (rather than only guarding the specific line that caused the reported symptom) is the complete fix: nothing about selection/command input should ever run while a ghost is being positioned. (d) Confirmed via the human's own reported repro numbers (zoomSpeed 0.2, min 2, max 20, one notch jumping to an extreme) that Windows' Input System `Mouse.scroll` reports ±120 per physical notch (matching the legacy `WHEEL_DELTA` constant), not a small ±1-ish value — `zoomSpeed` was effectively being multiplied by an unintended ~120× per notch. Converting in `Update()` (not inside `Zoom` itself) keeps `Zoom`'s existing contract and all of its existing tests unchanged — `Zoom` still means "shrink orthographic size by `notches * zoomSpeed`," exactly as already tested; only the raw-device-to-notches conversion is new, and it's its own pure/testable function rather than inline arithmetic | (a) Flipping `transform.localScale.x` on the prefab root (the human's first suggestion) — simplest to wire, and likely harmless given this project's simple symmetric colliders, but carries an unnecessary (if small) risk for any future code reading scale, and was worth countering per the human's own invitation; grouping `Visuals`+`Outline` under a new scaled parent (the human's second suggestion) — solves the same collider/logic-scale concern `flipX` already solves for free, at the cost of a prefab hierarchy change and a new Transform to keep positioned at local zero, for no benefit `flipX` doesn't already provide. (b) Applying the deferred-launch pattern to melee too, "for consistency" — melee has no projectile travel time to visually justify deferring; instant-on-tick already reads correctly and wasn't asked for. Polling the Animator's `normalizedTime` every frame in `Update()` to detect the release moment instead of an Animation Event — exactly the kind of per-frame polling this project's own conventions (`Update discipline: ... no polling that an event can replace`) already rule out; Animation Events are the built-in, zero-polling mechanism for precisely this. (c) Guarding only the specific right-click branch that produced the reported symptom — would leave the mirror-image left-click/deselection bug in place, an incomplete fix for a fully-diagnosed shared root cause. (d) Re-tuning `zoomSpeed` downward instead (e.g. asking the human to use ~0.0017) — technically works but bakes a magic, unexplained, hardware-specific constant into designer-facing data instead of fixing the actual unit mismatch in code, and would silently break again for any input device reporting a different raw delta convention |
| 70 | Superseded decision #69(c): the human reported the placement/selection race was *still* happening after that fix, correctly diagnosing the cause themselves (`PlacementController`/`SelectionController`'s `Update()`s have no guaranteed execution order, so decision #69(c)'s `IsPlacing` guard only worked in whichever frames Placement happened to run second) and offering two ways forward — accept clearing the selection whenever a placement starts (simplest, with newly-placed buildings auto-selecting as an explicitly-accepted side effect), or make the `IsPlacing` check itself non-racy. Implemented the first: removed `SelectionController`'s `PlacementController` reference and `IsPlacing` guard entirely; `SelectionController` now independently subscribes to the same `BuildingCatalogEntryEventChannel` `PlacementController` already reacts to (`HandleProduceRequested`), and clears the current selection there | The race can only produce a wrong result when a *stale* selection exists for Placement's click-handling to accidentally act on — remove the stale selection at the moment placement starts, and the ordering between the two controllers' `Update()`s stops mattering at all, for either click. This is a strictly better fix than trying to make the existing guard deterministic: Script Execution Order (`ProjectSettings/`) could force a reliable ordering, but touching `ProjectSettings/` needs explicit human approval per CLAUDE.md's asset-permission tiers, and even with reliable ordering the guard would still have been solving the problem from the wrong end (racing to check state instead of removing what the race could act on). Reusing `BuildingCatalogEntryEventChannel` — the exact channel `PlacementController` already subscribes to for the same trigger — costs zero new coupling between Placement and Selection (neither references the other, mirroring `BuildingRemovalRequestedEventChannel`'s existing multi-subscriber precedent, decisions log #55) and is strictly simpler than the reference-plus-guard shape decision #69(c) introduced, which is now removed rather than left alongside a redundant mechanism | Making decision #69(c)'s ordering reliable via Script Execution Order — floated by the human themselves as the alternative, and explicitly not pursued without asking first since it touches `ProjectSettings/`; a centralized click-routing/consumption system so only one controller ever "claims" a given click — the human's own second option, explicitly declined as too large an overhaul for what a much smaller, already-established decoupling pattern (event channels) fully solves; keeping decision #69(c)'s guard *alongside* this new fix as extra defense-in-depth — rejected as needless redundancy once the guard no longer does anything the event-driven clear doesn't already cover for the reported cases, and CLAUDE.md's smallest-correct-diff preference favors deleting code that no longer earns its keep over leaving a partially-effective mechanism in place |
| 71 | Small hygiene fix (human-flagged, no report filed — "skip the full report"), 2026-07-23: `GridView`/`IslandTerrainView`'s `OnValidate` no longer call `RebuildMesh`/`RebuildTerrain` synchronously — each now defers via `UnityEditor.EditorApplication.delayCall` to a `DeferredRebuildMesh`/`DeferredRebuildTerrain` method (guarded by `this == null` in case the object is destroyed before the callback fires), wrapped in `#if UNITY_EDITOR` since `EditorApplication` isn't available in player builds | Eliminated a cosmetic-but-noisy pair of "SendMessage cannot be called during Awake, CheckConsistency, or OnValidate" console warnings — `Tilemap.SetTile`/`MeshFilter.sharedMesh` assignment both internally use `SendMessage`, which Unity forbids while `OnValidate` is still running mid-serialization. The tile/mesh writes always completed correctly regardless (Unity finishes the write and only warns), so this was purely a console-noise fix, not a functional bug — deferring one editor tick is the standard, minimal-footprint fix for this exact class of warning. `Awake`/the existing Edit-Mode `Update` polling path (both already established, decisions #29/#64/#67) were left synchronous since only `OnValidate`'s specific mid-serialization timing triggers the warning — the generic warning text lists Awake too, but no warning was actually observed from that path. Human confirmed no warnings after manual testing (Editor was open, so batchmode re-verification wasn't run) | Suppressing the warning via `Debug.unityLogger.filterLogType` or similar — hides the symptom rather than the cause, and would also hide genuinely new SendMessage misuse elsewhere; moving the rebuild logic out of `OnValidate` entirely into a custom Editor/`SerializedProperty` callback — far more invasive for a one-line-root-cause fix |
| 72 | Ranged-attack-cancel fix (Report 036, human-reported): moving a unit while its ranged attack animation is playing let the animation finish and fire its projectile anyway, since only the C# action coroutine was cancelled, not the independently-running Animator state machine. Fixed in `SoldierBase.CancelAction`: (a) `_pendingRangedTarget` is now unconditionally cleared, so `ReleaseAttack()` (the Animation Event callback) always no-ops if it still fires after a cancel; (b) if a release was actually pending (i.e. the Animator is genuinely mid-attack, not idle/moving), `animator.Play(_idleStateHash, 0, 0f)` forces the state machine straight back to Idle instead of letting the Attack state's own exit-time transition run its course. `_idleStateHash` is captured once in a new `Start()` (not `Awake` — the Animator's default state isn't guaranteed evaluated that early) via `GetCurrentAnimatorStateInfo(0).fullPathHash`, relying on the existing fact that every wired controller's default state is Idle (decision #68) | (a) alone (clearing the pending target) would already stop the projectile from firing, but leaves the attack animation playing out to completion after the unit has visibly already left — looks broken even without a projectile. (b) needed a way to interrupt the Attack state on demand without hardcoding any of the 3 controllers' actual state names (they differ per soldier: `Archer_Shoot_Blue`, etc.) or touching the controller assets again — capturing the live Idle state's hash once at `Start()` sidesteps both a naming assumption and a further throwaway-script controller edit; `Animator.Play` is a direct state jump, not a played-through transition, so it can't re-trigger `ReleaseAttack`'s mid-clip event a second time. Gating the forced jump on `_pendingRangedTarget != null` (rather than always forcing Idle on every cancel) avoids visually restarting an already-playing Idle/Run loop on every ordinary move-order reissue — it only fires on the one case that's actually broken (a ranged attack mid-flight) | Only clearing `_pendingRangedTarget` without forcing the animation to stop — half-fixes the bug (no projectile) but leaves the animation itself looking uncancelled, contrary to the explicit ask ("the animation is immediately canceled"); adding a dedicated "Cancel" trigger + transitions to all 3 Animator Controllers (a further throwaway-script edit) — workable, but Trigger-based cancellation carries real staleness risk (a Cancel trigger set while not in the Attack state stays queued and can fire unexpectedly the next time Attack state is entered) that the `Start()`-captured-hash + direct `Play()` approach avoids entirely with zero controller changes; hardcoding each controller's actual Idle state name string in `SoldierBase` — works but is fragile (breaks silently if a state is ever renamed) and needlessly couples shared code to per-controller authoring details |
| 73 | Selection outline/animated-sprite desync fix (Report 037, human-reported): `GameEntityBase` gained a `LateUpdate` that syncs `outlineRenderer.sprite = spriteRenderer.sprite` whenever the outline is enabled, instead of the outline staying frozen on the sprite it was given at `Initialize` time | Animated soldiers (Report 033) reassign `spriteRenderer.sprite` every frame via their `Animator`; the outline never learns about it, so a selected, moving/attacking unit visibly showed its outline in a different pose than its actual sprite. Reading the live `spriteRenderer.sprite` each frame needs no duplicate Animator/Controller on the outline object at all — it just mirrors whatever the real renderer is already showing, so it can never drift out of sync by construction. Gated on `outlineRenderer.enabled` since there's no visible effect (and no reason to spend even a cheap reference-copy) on an unselected entity. Confirmed with the human first that this can't regress the draw-call budget (GI-12, decisions #18/#49/#64): the outline already renders via its own dedicated material or (decision #56) — swapping which sprite/frame it points at doesn't add a renderer, a material, or a texture (all sprites/frames share `SpriteAtlas_Gameplay`, decision #50), so it changes only *what* an already-existing draw call shows, not how many draw calls happen | Duplicating the `Animator`/state machine onto the outline object so it animates independently — solves the same problem but re-fires the entire per-controller wiring cost (decisions #68(b)) for a second renderer, for no benefit over just reading the value the real renderer already computed; replacing the outline entirely with a non-pose-dependent indicator (a ring/arrow under the unit) — the human's own second suggestion, not pursued since it would abandon the per-pixel-silhouette outline design decision #56 deliberately chose, for a bigger visual redesign across all 5 entity types just to route around a gap a one-line sync closes directly |
| 74 | Self-attack fix (human-flagged, no report filed — "skip the full report"), 2026-07-23: `SelectionController.HandleRightClick` now skips a selected soldier entirely (`continue`, neither attacks nor moves) when the right-clicked `hitTarget` is that same soldier | A right-click on a cell occupied by one of the currently-selected soldiers themselves was passing that soldier as its own `hitTarget` straight into `soldier.Attack(hitTarget, ...)` — the project has no faction/team system (nothing in `GameEntityBase`/`IDamageable` distinguishes friend from foe, by design/scope), so nothing else was stopping a soldier from being handed itself as an attack target and ticking damage into itself indefinitely. Skipping only applies to the one soldier that matches `hitTarget`; other selected soldiers in the same click still process it normally (attack it if it's a valid target for them, or move) | Adding a faction/team system to distinguish "self" more generally — far larger scope than this bug needs, and nothing in the brief asks for one; falling back to `MoveTo(cell)` for the self-targeted soldier instead of a full no-op — rejected as an assumption the player wanted to move that specific soldier, when they clicked on an entity, not empty ground |
